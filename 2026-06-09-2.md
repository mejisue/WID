# 2026-06-09 — 컨텍스트 정리 #2

## 작업

### WID 스킬 수정 — 로컬 파일 삭제 추가
- `/Users/soo/.claude/skills/WID/skill.md` 수정
- push 완료 후 해당 날짜 파일(`YYYY-MM-DD*.md`) 삭제하는 Step 6 추가
- Step 7로 결과 출력 단계 번호 조정

### morning_briefing.py — crawl_results 제거 및 sentinel 방식 변경
- `crawl_results/` 폴더: 플랫폼별 JSON 저장 + 중복 실행 방지 sentinel 두 가지 역할을 하고 있었음
- sentinel을 `last_run.txt` 단일 파일로 교체 (오늘 날짜 한 줄 기록)
- JSON 저장 블록(703~706번 줄) 제거
- `crawl_results/` 폴더 삭제 (`jasoseol/jobkorea/saramin/wanted-2026-06-09.json` 4개 있었음)
- save_to_tracker() 호출 직후 `last_run_file.write_text(today)` 추가

### 취업 폴더 정리 (`/Users/soo/Desktop/취업`)
- 삭제한 것: `calendar_events-*.json` 8개, `debug_api.py`, `debug_crawl.py`, `debug_output/`, `__pycache__/`, `.DS_Store`, `.env.example`, `briefings/`, `candidates.md`, `token.json`, `credentials.json`, `backfill_calendar.py`, `dedup_calendar.py`, `sync_calendar.py`, `frontend_summary.py`, `setup.sh`
- 남긴 것: 크롤러 파일들(`crawl.py`, `saramin_crawl.py`, `jasoseol_crawl.py`, `wanted_crawl.py`), `morning_briefing.py`, `run_briefing.sh`, `service_account.json`, `.env`, `calendar_event_map.json`, `지원-트래커.md`, `CLAUDE.md`

### retweet 프로젝트 — Sentry 프론트엔드 도입 (`feat/sentry-frontend`)
- 브랜치 생성: `feat/sentry-frontend`
- 패키지 설치: `pnpm add @sentry/react`, `pnpm add -D @sentry/vite-plugin`
- `src/sentry.ts` 생성: `Sentry.init()` 초기화, React Router v7 통합 (`reactRouterV7BrowserTracingIntegration`)
- `main.tsx`: `import './sentry'` 추가, `createRoot`에 React 19 에러 핸들러 적용 (`onUncaughtError`, `onCaughtError`, `onRecoverableError`)
- `App.tsx`: ErrorBoundary 방식 → React 19 핸들러 방식으로 교체하면서 Sentry 관련 코드 제거 (원상복구)
- `api/client.ts`: 401 외 API 에러에 `Sentry.captureException()` 추가
- `root-route.tsx`: `Routes` → `SentryRoutes`(`withSentryReactRouterV7Routing` 래핑)으로 교체
- `vite.config.ts`: `sentryVitePlugin` 추가, `sourcemap: true` 설정
- `.env.production`: `VITE_SENTRY_DSN` 추가
- `.env.local`: `VITE_SENTRY_DSN` 추가 (로컬 테스트용)
- 테스트: `enabled: true`로 임시 변경 후 테스트 버튼으로 에러 발생 → Sentry 대시보드 캡처 확인 → 원복
- 커밋 완료: `feat: Sentry 프론트엔드 에러 모니터링 도입`

## Q&A / 배운 것

### 소스맵(Source Map)이란
- 빌드 후 난독화된 코드(`index-Bx3kR9.js:1:4821`)를 원본 코드(`src/api/client.ts:23:5`)로 역추적하는 대응표
- `@sentry/vite-plugin`이 빌드 시 자동으로 Sentry 서버에 업로드
- 소스맵 자체는 배포 파일에 포함되지 않아 원본 코드 노출 없음

### Sentry DSN
- "Data Source Name" — 에러를 어느 Sentry 프로젝트로 보낼지 알려주는 주소
- Sentry Settings → Projects → Client Keys(DSN)에서 확인

### React 19 에러 핸들러 vs ErrorBoundary
- ErrorBoundary: React 18 이하 방식, 렌더링 크래시만 잡음
- React 19: `createRoot(container, { onUncaughtError, onCaughtError, onRecoverableError })`으로 더 낮은 레벨에서 잡음
- 공식 문서: https://react.dev/reference/react-dom/client/createRoot

### React Router v7 Sentry 통합
- 단순 `browserTracingIntegration`만으론 페이지 이동 트레이싱 안 됨
- `reactRouterV7BrowserTracingIntegration` + 라우팅 방식에 맞는 래핑 필요
- 이 프로젝트는 `<Routes>` 방식 → `withSentryReactRouterV7Routing(Routes)`으로 래핑
- 공식 문서: https://docs.sentry.io/platforms/javascript/guides/react/features/react-router/v7/

### Sentry `enabled` 옵션
- `import.meta.env.PROD`로 프로덕션에서만 전송 활성화
- 개발 환경에서 테스트하려면 임시로 `true`로 변경 후 확인

## 미해결
- [ ] retweet 백엔드(Spring Boot) Sentry 적용
- [ ] `feat/sentry-frontend` PR 작성
- [ ] `SENTRY_AUTH_TOKEN` CI/CD 환경변수 등록 (소스맵 업로드용)
