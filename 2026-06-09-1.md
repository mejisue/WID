# 2026-06-09 — 컨텍스트 정리 #1

## 작업

### 시작! 트리거 정리
- 완료된 메모리 삭제: project_portfolio_modal_work.md, project_design_system.md
- 어드민 대시보드 시각화 작업 시작

### BE — 어드민 대시보드 통계 API 구현
- `feat/admin-dashboard` 브랜치 사용 (develop 기준 rebase)
- `Post.java`에 `viewCount` 필드 추가, `incrementViewCount()` 메서드 추가
- `PostService.getPost()` — readOnly 제거 + 조회 시 viewCount 자동 집계
- `PostRepository`에 JPQL 쿼리 3개 추가
  - `findMonthlyPostCounts()`: 연/월/개수 GROUP BY
  - `findTopTags(Pageable)`: 태그별 개수 Top 10
  - `findTop5ByOrderByCreatedAtDesc()`: 최신 글 5개
- DTO 4개 신규: `MonthlyPostCount`, `TagCount`, `RecentPost`, `AdminStats`
- `AdminController` 개편: `GET /api/admin/stats` 엔드포인트 추가
- 빌드 통과 (`./gradlew compileJava`)

### BE — 테스트 작성
- `PostServiceTest` — `게시물_단건_조회_성공`에 viewCount 증가 검증 추가
- `AdminControllerTest` 신규 작성 — 3개 테스트
  - `X-ADMIN-SECRET: test-secret` 헤더 필요 (누락으로 403 → 수정)
  - `application-test.yaml` 첫 줄 `ㅂ` 오타 수정
- 전체 테스트 통과 (20 tests)
- 커밋: `feat: 어드민 대시보드 통계 API 추가 및 조회수 집계 기능 구현`

### FE — 어드민 대시보드 구현
- `pnpm add recharts` (이미 설치되어 있었음)
- `AdminDashboard.tsx` 신규 생성 ("use client")
  - StatCard × 3, BarChart(월별), HorizontalBarChart(태그), 최근 게시물 테이블
- `app/admin/page.tsx` 리빌드 — `/api/admin/stats` fetch + AdminDashboard에 props 전달
- 타입체크 통과
- 커밋: `feat: 어드민 대시보드 FE 구현`

### FE — 디자인 개선 (여러 차례 반복)
- 레퍼런스 이미지 기반 리디자인: 파스텔 톤, 원형(도넛) 차트
  - StatCard: 흰 배경 + 상단 파스텔 컬러 라인 (좌측→상단으로 변경)
  - BarChart: 연보라(`#C4B5FD`), 수평 점선 그리드, 축선 제거
  - 태그 분포: 수평 바차트 → 도넛 PieChart (파스텔 민트/연두 계열)
  - 최근 게시물: 테이블 → 리스트 카드형, `/post/{id}` 링크 연결
  - 배경: `bg-gray-50`
- 텍스트 가시성 개선: `text-gray-400` → `text-gray-600` 일괄 변경
- Legend 글자색: formatter로 `#374151` 고정 (아이콘 색 유지)
- 모바일 레이아웃: `min-w-[550px]` (사용자가 수정, 900px → 550px)
- 커밋: `style: 어드민 대시보드 디자인 개선`

### PR 생성 및 배포
- PR #46: `feat/admin-dashboard` → `develop` (GitHub)
- main 브랜치 머지 시 에디터 오류 (`code --wait: command not found`)
  - 해결: `git commit -m "..."` 으로 직접 메시지 전달
- `git push origin main` non-fast-forward → `git pull origin main --no-rebase` 후 해결
- EC2 Docker 배포 완료

### 배포 후 에러 트러블슈팅
- 에러: `통계 데이터를 불러올 수 없습니다.`
- 원인 1: `ddl-auto: validate` 환경에서 viewCount 컬럼 미생성
  - 해결: EC2 MySQL 컨테이너 접속 후 `ALTER TABLE post ADD COLUMN view_count INT NOT NULL DEFAULT 0` 수동 실행
- 원인 2: Vercel에 `BACKEND_SECRET_KEY` 환경변수 미등록
  - 해결: Vercel 대시보드에서 추가 후 재배포

### Morning Briefing 크롤링 미실행 이슈
- 원인: Mac을 오전 8:41에 켰는데 9시 이전 가드에 걸려 종료
  - `RunAtLoad`라 오늘 재실행 없음
- 추가 원인: Playwright chromium 미설치 (잡코리아 실패)
  - 해결: `playwright install chromium`
- 9시 이전 가드 제거 (`morning_briefing.py` 626~629번 줄 삭제)
- `launchctl kickstart -p gui/$(id -u)/com.soo.morning-briefing` 수동 실행

### 경험 블록 업데이트
- 블록 W 신규 추가: 어드민 대시보드 시각화 (경험-블록-인덱스.md, 경험-블록-통합.md)

---

## Q&A / 배운 것

### CSS 특이성 — tr에 text-muted + th에 text-gray 충돌
- `tr`에 color가 직접 적용되면 `th`에 같은 특이성 클래스를 줘도 Tailwind 선언 순서에 따라 오버라이드 안 될 수 있음
- 해결: `tr`에서 color 클래스를 빼고 `th`에만 부여

### 디자인 커뮤니케이션 방법
- 레퍼런스 스크린샷 던지기가 가장 효과적
- 분위기 단어("파스텔", "미니멀"), 불만 기반("글자 안 보여"), 요소 지목 모두 가능

### Recharts Tooltip 텍스트 색 설정
- `itemStyle={{ color: "#374151" }}`, `labelStyle={{ color: "#374151" }}`
- `Bar`의 `name` prop이 Tooltip 라벨로 표시됨

### ddl-auto: validate 환경에서 컬럼 추가
- Hibernate가 자동 생성 안 함 → 수동 ALTER TABLE 필요
- `ddl-auto: update`면 배포 시 자동 반영됨

### Docker 환경에서 localhost 문제
- Next.js(Vercel)에서 Spring Boot(EC2)로 fetch 시 localhost가 아닌 실제 도메인/IP 사용해야 함

### Sentry vs Amplitude
- Sentry: 에러 트래킹 — "뭐가 부서졌나?" (개발자 관점)
- Amplitude: 사용자 행동 분석 — "사용자가 뭘 하나?" (제품 관점)
- 채용 어필: "배포 후 책임지는 개발자" 증명 수단
- 추천: Retweet에 둘 다, myWebsite에 Sentry만, Sleact Sentry만

---

## 미해결
- [ ] Google Calendar 403 에러 지속 (캘린더 등록/삭제 실패)
- [ ] Morning Briefing 원티드/사람인 DNS 오류 (네트워크 늦게 뜨는 시점 문제 추정)
- [ ] Sentry/Amplitude Retweet 실제 도입
