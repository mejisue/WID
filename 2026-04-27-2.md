# 2026-04-27 — 컨텍스트 정리 #2

## 작업

### EC2 CPU 크레딧 고갈 → 인스턴스 접속 불가 진단
- `CPUCreditBalance` CloudWatch 지표 확인 → 거의 0 (0.00027, 0.000043...)
- 어제 t3.medium → t3.small 복귀 + 힙 제한 작업 중 크레딧 소진
- t3.small CPU 크레딧 적립 속도: 시간당 24개, 베이스라인 20% 이하여야 적립
- 해결 방법: 콘솔에서 무제한 크레딧 모드 ON → 접속 후 원인 파악

### t3.small → t4g.medium 업그레이드 시도 → t3.medium으로 결정
- t4g(Graviton/ARM64) 시도했으나 현재 AMI가 x86 전용 → 인스턴스 유형 변경 드롭다운에서 t4g 선택 불가
- t4g 쓰려면 ARM64 AMI로 새 인스턴스 파야 함
- → t3.medium으로 결정 (어제 검증됨, 월 ~$23)
- GitHub Actions 워크플로우에 `platforms: linux/arm64` 추가했다가 롤백

### GitHub Actions 워크플로우 개선 (myWebsite, retweet, sleact 3개)
- `feat/arm64-build-support` 브랜치에서 arm64 빌드 추가 → main 머지
- arm64 롤백: `feat/revert-arm64-build` 브랜치에서 제거 → main 머지
- `chore/auto-git-pull-on-deploy` 브랜치: 배포 스크립트에 `git pull origin main` 추가
  - EC2에서 compose 파일 변경 시 수동 pull 불필요해짐
- sleact 브랜치에서 반복적으로 경로 에러 발생: 병렬 Bash 실행 시 `cd` 없이 git add 하면 이전 명령의 working directory에서 실행됨 → 항상 `cd /path &&` 명시 필요

### myWebsite 500 에러 원인 파악
- `GET https://mejisue.site/post 500` → Spring Boot API 오류
- 에러 원인: EC2 CPU 크레딧 고갈로 Spring Boot 응답 불가
- 코드 분석: `GlobalExceptionHandler`가 `IllegalArgumentException`만 처리, DB 오류는 잡지 않음

### EC2 nginx 구조 파악
- nginx 설정 파일 위치: `/etc/nginx/sites-enabled/` (mywebsite, retweet-api, sleact-api)
- mywebsite: `listen 80` + `proxy_pass localhost:8082` (server_name 없음 = 기본 서버)
- retweet-api: `server_name retweet-api.mejisue.site` → `localhost:8080`
- sleact-api: `server_name sleact-api.mejisue.site` → `localhost:8083`
- retweet/sleact에 WebSocket 헤더 추가 (`Upgrade`, `Connection: upgrade`)

### myWebsite 반응형 UI 개선 (feat/reactive-web-ui)
- 문제: 이모지 크기 고정(rem) + 절대 위치(%) → 화면 좁아지면 겹침
- 해결: `useIsMobile()` 훅으로 모바일/데스크탑 분기, 각 아이템에 `mobilePos`/`mobileSize` 추가
- 480px 미만에서 `min-width: 480px` + `body { overflow-x: hidden }` → 레이아웃 고정 후 클립
- 아이템 위치 조정: 🧵 말풍선 영역 위로 이동 (top: 7%), 📒 블로그 바닥으로 이동
- 데코 아이템 정리 (shelf, clock, flask, ctrl 제거), 🎮 자리에 📒 배치
- PR #32 → main 머지

### develop 브랜치 동기화 문제
- `git pull origin main` → diverged branches 에러
- develop에 `#7~#22` 커밋들이 origin/main에 없음 (별도 경로로 main에 반영된 적 없음)
- rebase 시도 → 충돌 12개 커밋마다 반복 → abort
- `git merge origin/main` 전략으로 변경 → 충돌 한 번에 해결 (origin/main 버전 채택)
- rebase에서 `--ours`/`--theirs` 방향이 merge와 반대임 (rebase에서 `--ours` = origin/main)

### 무한 스크롤 기능 main 반영 (PR #33)
- develop에만 있던 `#22 무한 스크롤` 기능 → PR #33으로 main 머지
- 빌드 실패: `react-intersection-observer` 패키지 누락 → `pnpm add` 후 커밋
- 빌드 실패: `PostPage` 타입, `getPostsPage` 함수 `posts.ts`에 없음 → 추가
- `PostPage`: `{ content: PostSummary[], page: number, size: number, hasNext: boolean }`
- `getPostsPage(page, size)`: `/posts?page=&size=` API 호출

### CV 업데이트 (feat/favicon-cv → PR #34)
- 기존 cv.pdf에 `file:///Users/soo/Downloads/이력서_김지수_v3.html` + 날짜/제목 헤더 표시 문제
- 원인: 브라우저 인쇄 시 자동 추가되는 URL 헤더/푸터
- 해결: HTML에 `@page { margin: 15mm; }` 추가 + Chrome headless `--no-pdf-header-footer` 옵션으로 재생성
- 메타데이터 Frontend → FullStack으로 수정, favicon.ico 제거

### favicon 개선 (fix/favicon)
- `app/favicon.ico` 삭제 후 `app/icon.png` 추가 → Next.js App Router 자동 처리
- 문제: 아이콘이 작아 보임
- 원인: 파란 원 배경 + 여백으로 고양이가 캔버스의 60% 미만 차지
- favicon이 커 보이는 이유 = 배경 색깔이 아니라 아이콘이 캔버스를 얼마나 채우는지
- 현재 검은 배경 + 원이 꽉 찬 버전으로 교체 진행 중

---

## Q&A / 배운 것

### sudo apt update
- `apt`: Ubuntu 패키지 관리자. `update`는 설치 가능한 패키지 목록 최신화 (실제 설치/업그레이드 아님)
- `sudo`: 관리자 권한 실행

### nginx 기본 문법
- `server {}`: 하나의 가상 서버 정의
- `listen 80`: 해당 포트로 오는 요청 수신
- `server_name`: 같은 포트에서 여러 도메인 구분. 없으면 default server
- `location /`: URL 경로별 처리 규칙
- `proxy_pass`: 요청을 다른 서버로 전달 (리버스 프록시)
- `proxy_set_header`: 전달 시 헤더 추가 (실제 클라이언트 IP 등)

### Let's Encrypt 인증서
- HTTPS를 무료로 제공하는 인증서 발급 기관
- `certbot --nginx -d 도메인`으로 발급 + nginx 설정 자동 추가 + 자동 갱신
- 유효기간 90일

### 실무 브랜치 전략
- `feat/* → develop → staging → main(production)` 3단계 환경
- main은 항상 배포 가능한 상태 유지
- 프로덕션 긴급 수정: `hotfix/*` 브랜치를 main에서 분기 → main 머지 → develop 동기화
- 오늘 `react-intersection-observer` 누락, `PostPage` 타입 에러가 hotfix 케이스

### rebase vs merge 충돌 해결 방향 차이
- merge: `--ours` = 내 브랜치, `--theirs` = 합치려는 브랜치
- rebase: `--ours` = rebasing onto 브랜치(origin/main), `--theirs` = 적용 중인 커밋(develop)
- 즉 rebase에서 origin/main 버전 유지하려면 `--ours` 사용

### favicon이 커 보이는 조건
- 배경 색이 아니라 아이콘이 캔버스(이미지 전체 크기)를 얼마나 채우는지가 핵심
- 여백/패딩이 많으면 탭에서 작아 보임
- Next.js App Router: `app/icon.png` 배치 시 자동으로 favicon 처리 및 다양한 해상도 생성

---

## 미해결
- [ ] favicon 검은 배경 버전으로 교체 및 PR 머지
- [ ] t3.medium 업그레이드 (현재 CPU 크레딧 고갈 상태, 인스턴스 접속 불가)
- [ ] myWebsite 500 에러 근본 원인 확인 (EC2 접속 후 Spring 로그 확인 필요)
