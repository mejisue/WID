# 2026-06-10 — 컨텍스트 정리 #1

## 작업

### retweet 프로젝트 Sentry 도입 (프론트 + 백엔드)
- 한 일:
  - 프론트엔드: 이미 작업되어 있던 `feat/sentry-frontend` 브랜치를 push하고 PR #25 생성 (develop 대상)
    - `@sentry/react` + `@sentry/vite-plugin`, `src/sentry.ts` 초기화(React Router v7 통합), `main.tsx` React 19 에러 핸들러, `api/client.ts` axios 인터셉터에서 에러 캡처, vite.config.ts 소스맵 업로드 설정
  - 백엔드: `feat/sentry-backend` 브랜치에서 작업
    - `build.gradle`에 `sentry-spring-boot-starter-jakarta:8.43.1` 추가
    - `application.yaml`에 `sentry.dsn/environment/traces-sample-rate/send-default-pii` 설정 (env var `SENTRY_DSN`)
    - `application-local.yaml`은 로컬 테스트용 DSN 직접 입력 (gitignore 대상)
    - `GlobalExceptionHandler`의 catch-all `Exception` 핸들러에 `Sentry.captureException(e)` 추가 — Spring 자동 캡처가 동작하지 않는 이유는 이 핸들러가 모든 예외를 "처리됨"으로 만들기 때문
    - `.env.example`, `backend/docker-compose.prod.yml`에 SENTRY_DSN 추가 (다만 이 compose 파일은 실제 배포에 안 쓰이는 레거시 파일로 확인됨)
  - 로컬에서 임시 엔드포인트(`GET /api/auth/sentry-test`, RuntimeException throw)로 Sentry 수신 확인 후 제거 → 커밋(7188917) → push → PR #26 생성 (develop 대상)
  - 컴파일 확인: `./gradlew compileJava` 성공

- 현재 상태: 프론트(PR #25)·백엔드(PR #26) 모두 main까지 머지 완료

### 운영 환경 SENTRY_DSN 적용
- 한 일:
  - Vercel: `VITE_SENTRY_DSN` 환경변수를 Production/Preview에 등록 (CLI로 `vercel env ls production` 확인)
  - EC2: 실제 운영 compose는 루트 `compose.prod.yaml` (백엔드 폴더 안의 docker-compose.prod.yml이 아님). `app` 서비스가 `env_file: .env.prod`로 환경변수 주입하므로 `.env.prod`에 `SENTRY_DSN=...` 한 줄 추가만으로 충분
  - `.github/workflows/deploy-backend.yml` 분석: main push 시 GHCR에 이미지 빌드/푸시 → EC2 SSH → git pull → `docker compose --env-file .env.prod -f compose.prod.yaml pull/up -d app`
  - EC2에서 `docker compose --env-file .env.prod -f compose.prod.yaml down` → `up -d`로 재배포 완료

- 문제 / 에러:
  - Vercel 배포 중 `sh: line 1: vite: command not found`, `Command "vite build" exited with 127` 발생
  - 원인: 빈 커밋("Trigger build")으로 재배포 시 Vercel이 `Skipping build cache since Package Manager changed from "pnpm" to "npm"` 로그를 남기며 의존성 설치 단계 자체를 건너뜀. `front/pnpm-lock.yaml`은 정상 존재 — Vercel 플랫폼 쪽 일시적 캐시/감지 오류로 추정
  - 해결: 직전(17분 전) 빌드는 동일 파일 상태로 정상 성공했으므로, 캐시 없이 재배포하면 해결될 것으로 안내 (사용자가 재배포 진행)

- 검증:
  - 백엔드: `permitAll`인 `/api/auth/login`에 깨진 JSON(`'not-json'`) POST → `JsonParseException`(HttpMessageNotReadableException의 root cause) → catch-all 핸들러 → Sentry 캡처 확인됨, environment=production
  - 프론트: 배포 사이트 콘솔에서 `setTimeout(() => { throw new Error("Sentry frontend production test") })` 실행 → Sentry에 반영 확인됨 (전역 window.onerror 핸들러가 캡처)

- 현재 상태: 프론트/백엔드 모두 운영 Sentry 연동 및 에러 캡처 검증 완료. 작업 종료.

## Q&A / 배운 것

### 백엔드 Sentry 자동 캡처 vs 수동 캡처
- Spring Boot의 `sentry-spring-boot-starter`는 설정 기반 auto-configuration이라 `Sentry.init()` 코드 호출 불필요
- 단, 프로젝트에 모든 예외를 잡는 `@ExceptionHandler(Exception.class)` catch-all이 있으면 Sentry 입장에서 "이미 처리된 예외"로 보여 자동 캡처가 안 됨 → 그 안에서 `Sentry.captureException(e)` 직접 호출 필요 (프론트의 axios 인터셉터 수동 캡처와 같은 패턴)

### 프론트 vs 백엔드 Sentry 설정 비교
- 환경별 on/off: 프론트는 `enabled: import.meta.env.PROD`(코드), 백엔드는 `application-local.yaml`에서 `sentry.dsn: ""`(DSN 비우면 starter 자동 비활성화)
- environment 태그: 프론트 `import.meta.env.MODE`, 백엔드 `SENTRY_ENVIRONMENT` env (기본 production)
- 소스맵: 프론트만 필요 (JVM은 스택트레이스에 클래스/라인 정보가 그대로 남음)
- PII: 백엔드 `send-default-pii: false` 명시 (프론트 SDK 기본값도 false라 별도 설정 불필요)

### Vite 환경변수와 Vercel
- `import.meta.env.VITE_*` 형태만 클라이언트 번들에 포함됨. `SENTRY_DSN`처럼 `VITE_` 접두사 없는 변수는 무시됨
- Vite 환경변수는 빌드 타임에 번들에 박히므로, Vercel에 환경변수 추가 후 반드시 재배포(rebuild) 필요

### docker compose env_file vs environment
- `env_file:`로 지정한 파일의 모든 key=value는 컨테이너 환경변수로 그대로 주입됨 (compose 파일에 `${VAR}` 형태로 명시할 필요 없음)
- `.env` 변경 후 적용하려면 컨테이너 재생성 필요: `docker compose --env-file .env.prod -f compose.prod.yaml down && up -d` (또는 `up -d --force-recreate <service>`)

### retweet 프로젝트 아키텍처
- Vercel: React 프론트(front/) 정적 배포
- EC2: Docker로 `retweet-app`(Spring Boot) + `retweet-redis`, MySQL은 호스트의 다른 프로젝트(sleact)와 공유(172.17.0.1)
- 배포: main push → GitHub Actions가 GHCR에 이미지 빌드/푸시 → SSH로 EC2 접속 → git pull(compose 동기화) → `docker compose --env-file .env.prod -f compose.prod.yaml pull/up -d app` (redis 유지)
- 운영 compose는 루트의 `compose.prod.yaml`이며, `backend/docker-compose.prod.yml`/`backend/.env.example`은 사용 안 하는 레거시 파일

## 미해결
- [ ] (선택) `backend/docker-compose.prod.yml`, `backend/.env.example`의 SENTRY_DSN 추가분 — 레거시 파일이라 영향 없으나 정리 여부는 미정
- [ ] Sentry 테스트로 발생시킨 이슈들(JsonParseException, "Sentry frontend production test") Sentry 대시보드에서 Resolve/Delete 처리 필요
