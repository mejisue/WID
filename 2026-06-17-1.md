# 2026-06-17 — 컨텍스트 정리 #1

## 작업

### myWebsite 정적 렌더링/ISR 도입
- 배경: 프로젝트가 SSR(Server Component)은 쓰고 있었으나, 모든 데이터 fetch가 `cache: 'no-store'` → 방문할 때마다 백엔드(Spring Boot) 호출하는 완전 동적 렌더링이었음.
- 목표: 블로그 공개 페이지를 정적/ISR로 전환해 TTFB 감소 + 백엔드 부하 제거.
- 브랜치: `perf/static-blog-rendering` (develop에서 새로 땄음. 동기화 후 작업)

#### 변경 내용
1. `lib/api/posts.ts`
   - `getPosts`: `no-store` → `next: { tags: ['posts'], revalidate: 3600 }`
   - `getPost`: `no-store` → `next: { tags: ['posts', 'post-${id}'], revalidate: 3600 }`
   - `revalidatePosts(id?)` 헬퍼 추가 → create/update/deletePost 성공 후 `/api/revalidate` 호출
   - `getPostsPage`(클라 무한스크롤)는 no-store 유지
2. `app/api/revalidate/route.ts` (신규)
   - 어드민 세션만 통과(getServerSession). 이 사이트는 어드민만 로그인 가능 → 세션 존재 = 어드민
   - `revalidateTag('posts', 'max')` + `revalidateTag('post-${id}', 'max')`
3. `app/post/[id]/page.tsx`
   - `getServerSession` 제거 → 정적화
   - `generateStaticParams` 추가(빌드 시 전체 글 생성, 백엔드 다운 시 try/catch로 [])
4. `app/post/[id]/_components/PostAdminActions.tsx` (신규)
   - 수정/삭제 버튼만 클라이언트에서 `useSession`으로 로그인 확인 후 표시
5. `app/post/page.tsx`
   - 빌드 시 백엔드 다운 대비 try/catch 추가

#### 결과 (next build 라우트 테이블)
- `/` → ○ Static (revalidate 1h)
- `/post` → ○ Static (revalidate 1h)
- `/post/[id]` → ● SSG (generateStaticParams)
- `/admin/*`, `/api/*` → ƒ Dynamic (그대로, 맞음)
- 변경 전엔 위 공개 페이지가 전부 ƒ Dynamic이었음

#### 현재 상태
- 빌드 성공. 커밋은 아직 안 함(검토 후 결정).

## Q&A / 배운 것

### Next.js 16 revalidateTag 시그니처 변경
- 문제: `revalidateTag('posts')` → 빌드 타입 에러 "Expected 2 arguments, but got 1."
- 원인: Next 16부터 `revalidateTag(tag, profile)` 두 번째 인자 필수. 단일 인자는 deprecated.
- 해결: `revalidateTag('posts', 'max')` — `'max'` = 즉시 만료. 또는 `updateTag` 사용 가능.

### 태그 기반 캐시 무효화 흐름
- mutation은 브라우저(클라)에서 실행, 캐시는 서버에 있음 → 클라가 서버 캐시 직접 못 버림
- 해결: 서버 API 라우트(`/api/revalidate`)를 창구로 두고 클라가 호출 → 서버에서 `revalidateTag` 실행

### 이 프로젝트 인증 구조
- next-auth `signIn` 콜백이 `ADMIN_EMAIL`만 로그인 허용 → 세션 존재 자체가 어드민 증명
- 덕분에 클라 어드민 판별에 NEXT_PUBLIC 이메일 노출 불필요, 세션 존재 여부만 확인

## 미해결
- [ ] 커밋/푸시 여부 결정
- [ ] 실제 백엔드 띄워서 동작 확인(/verify) — 글 수정 후 캐시 갱신 정상 작동하는지
- [ ] `formatTimeAgo`가 정적 페이지라 캐시 시점에 고정됨. 백엔드 `createdAt` 추가 후 절대 날짜 표기 권장(코드에 TODO 존재)
