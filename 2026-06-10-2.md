# 2026-06-10 — 컨텍스트 정리 #2

## 작업

### retweet 프로젝트에 Amplitude 사용자 행동 분석 도입
- `feat/amplitude-frontend` 브랜치 생성 (develop에서 분기)
- `@amplitude/analytics-browser` SDK 설치 (pnpm)
- `src/amplitude.ts` 생성 — `amplitude.init()`, autocapture로 pageViews/sessions만 활성화 (attribution, formInteractions, fileDownloads, elementInteractions는 끔). `defaultTracking`은 deprecated라 `autocapture` 옵션 사용
- `src/lib/analytics.ts` 생성 — `AMPLITUDE_EVENTS` 상수, `trackEvent()`, `identifyUser()`, `resetUser()` 헬퍼. 새 이벤트 추가 시 이 파일만 건드리면 되는 구조로 설계
- `main.tsx`에 `import './amplitude'` 추가
- 1차 핵심 이벤트 6종 트래킹 구현:
  - `sign_up` (use-sign-up.ts)
  - `sign_in` method: 'password' (use-sign-in-with-password.ts), method: 'github' (github-callback-page.tsx — oauth 콜백 완료 지점)
  - `sign_out` (use-sign-out.ts)
  - `post_created` (use-create-post.ts)
  - `post_like_toggled` — postId, isLiked 속성 (use-toggle-post-like.ts, onMutate에서 저장한 prevPost로 새 isLiked 계산)
  - `comment_created` — postId 속성 (use-create-comment.ts)
- "익명 사용자" 문제 발견 → Amplitude는 기본적으로 deviceId(랜덤 UUID) 기준이라 로그인 여부와 무관하게 익명 표시됨
  - 해결: `identifyUser(memberId)` / `resetUser()` 추가
  - `session-provider.tsx`(앱 최초 로드 시 refreshSession 성공/실패) + sign-up/sign-in(password/github)/sign-out 각 지점에 적용
- `.env.local`, `.env.production`, `.env.example`에 `VITE_AMPLITUDE_API_KEY` 추가 (.env.local은 gitignore, 사용자가 직접 키 입력)
- 로컬 dev 서버(5173) + 백엔드(8080) 띄워서 수동 검증 — 정상 동작 확인
- 커밋 + PR #27 생성 (mejisue/retweet, base: develop)
  - PR 본문 Test plan에는 typecheck/lint 항목 빼고 실제 동작 검증만 작성 (사용자 피드백)

### Vercel 배포 에러 진단 — "vite: command not found" (exit 127)
- 문제: Vercel 빌드에서 `sh: line 1: vite: command not found`, `Error: Command "vite build" exited with 127`
- 처음엔 Amplitude PR 때문인가 의심했으나, 빌드 로그 비교로 무관함을 확인
- 원인: Vercel 프로젝트 설정의 **Root Directory가 `front` → `.`(저장소 루트)로 변경**되어 있었음
  - 루트에는 package.json이 없어 install 단계가 사실상 빈 채로 끝나고, lockfile도 못 찾아 "Package Manager changed from pnpm to npm" 발생
  - 그 상태에서 `vite build` 실행 → vite 미설치로 command not found
- 증거: 3시간 전 main 브랜치 배포는 성공(front 의존성 정상 설치), 그 이후 main 재배포부터 동일 에러로 실패 → PR과 무관하게 그 사이 Vercel 설정이 바뀐 것
- 해결 방법 제시: Vercel 대시보드 → Settings → General → Root Directory를 `front`로 변경 후 재배포

## Q&A / 배운 것

### Amplitude란?
- 사용자 행동(이벤트) 기반 제품 분석 도구. Sentry(에러 모니터링)와 달리 "무엇을 했는지" 추적
- 핵심 개념: Event(행동 단위), Event Properties(이벤트 속성), User Properties(유저 속성) → 퍼널/리텐션 분석에 활용

### Amplitude 브랜치 분리 필요 여부
- Sentry는 프론트/백엔드 모두 SDK 연동이 필요해서 `feat/sentry-frontend`/`feat/sentry-backend`로 분리했었음
- Amplitude는 `analytics-browser`(클라이언트 SDK)만으로 충분 — 백엔드 분리 불필요, `feat/amplitude-frontend` 단일 브랜치로 진행

### Amplitude SDK v2 옵션 변경 사항
- `defaultTracking` 옵션은 deprecated, `autocapture`로 대체됨
- `autocapture.sessions`, `autocapture.pageViews` 등으로 세분화 설정 가능

## 미해결
- [ ] Vercel 프로젝트 Root Directory를 `front`로 수정 (대시보드 또는 API) — 현재 main/PR 배포 모두 실패 상태
- [ ] PR #27 머지 후 Vercel 환경변수에 `VITE_AMPLITUDE_API_KEY` 등록
- [ ] (선택) 2차 이벤트(post_updated/deleted, comment_updated/deleted, profile_updated, password_reset 등) 추가 여부 결정
