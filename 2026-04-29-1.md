# 2026-04-29 — 컨텍스트 정리 #1

## 작업

### 케이엘피 자소서 작성 및 수정

### 상태변경 스킬 업데이트 — 지원완료 시 캘린더 이벤트 제목 변경
- 기존: 탈락/포기만 캘린더 이벤트 삭제 처리
- 추가: 지원완료 시 `[마감] 회사명` → `[지원완료] 회사명` 업데이트
- SKILL.md Step 5에 "지원완료인 경우" 분기 추가

### 상태 변경 처리
- 케이엘피 → 지원완료
- 윈카드 → 탈락, 캘린더 이벤트 삭제 완료
- 이지케어텍 → 탈락, 3건(사람인/자소설/원티드) 전부, 캘린더 이벤트 3건 삭제 완료

### morning_briefing.py 버그 수정 3건
- 문제 1: Calendar 토큰 만료 시 unhandled exception → Slack 전송 막힘
  - 수정: `add_events_to_calendar()` try-except 처리
  - `invalid_grant` 감지 시 Slack으로 재인증 안내 자동 전송
- 문제 2: 9시 이전 / 오늘 이미 실행됨 체크로 강제 실행 불가
  - 수정: `--force` 플래그 추가
- 문제 3: `run_local_server()` → Claude Code 터미널에서 브라우저 못 띄움
  - 수정: `run_console()`로 교체, refresh 실패 시 except로 잡아 재인증 흐름으로 전환
  - 만료된 `token.json` 삭제

## Q&A / 배운 것

### Google OAuth token.json 구조
- `access_token`: API 호출용 단기 토큰 (1시간)
- `refresh_token`: access_token 자동 재발급용 장기 토큰 (token.json에 만료 시각 없음, Google 서버 관리)
- `expiry`: access_token 만료 시각

### Google OAuth 앱 테스트 모드 vs 프로덕션
- 테스트 모드(기본값): refresh_token 7일 후 자동 만료 → 1주일 된 프로젝트에서 만료된 원인
- 프로덕션: 6개월 미사용 시에만 만료
- 해결: Google Cloud Console → OAuth 동의 화면 → "앱 게시"

### run_local_server vs run_console
- `run_local_server(port=0)`: 브라우저 자동 오픈 → Claude Code 터미널에서 동작 안 함
- `run_console()`: URL 출력 후 인증 코드 붙여넣기 → 어느 환경에서나 동작

### launchd StartCalendarInterval
- Hour 없이 Minute만 지정 → 매 시간 해당 분에 실행
- Mac 슬립 중이면 해당 interval 무시, catch-up 없음

### /자소서 스킬이 myWebsite 경험만 편중하는 이유
- 경험 블록 인덱스에서 G/H만 `프론트엔드` 태그, 나머지는 `백엔드`/`인프라`
- 프론트엔드 JD → 프론트엔드 태그 블록만 우선 선택
- "처음부터 작성" 모드에서 편중, "검토·수정" 모드에선 원고 경험 활용
- 해결: JD 전문 붙여넣기 or 경험 블록 명시 or SKILL.md에 "풀스택 포함" 지침 추가

## 미해결
- [ ] Google OAuth 재인증 완료 (`python3 morning_briefing.py --force` 실행)
- [ ] Google Cloud Console OAuth 앱 "프로덕션"으로 게시
- [ ] 케이엘피 캘린더 이벤트 [마감] → [지원완료] 업데이트 (스킬 수정 이전 건)
