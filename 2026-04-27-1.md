# 2026-04-27 — 컨텍스트 정리 #1

## 작업

### EC2 t3.small 메모리 부족 → t3.medium 업그레이드 후 t3.small 복귀
- 웹사이트 접속이 너무 느린 문제 발생
- top 확인 결과 kswapd0가 CPU 51.4% 점유 → 메모리 부족으로 인한 스왑 thrashing
- 메모리 현황: Java x3 (~800MB) + MySQL x1 (~380MB) + 기타 → 여유 76MB
- t3.small(2GB) → t3.medium(4GB) 업그레이드 후 JAVA_OPTS 힙 제한 작업
- 힙 제한 후 t3.small로 복귀 → available 285MB로 개선

### Java 힙 제한 (-Xms128m -Xmx256m) 적용
- 3개 프로젝트(sleact, retweet, myWebsite) docker-compose에 JAVA_OPTS 추가
- 문제: Dockerfile ENTRYPOINT가 `["java", "-jar", "app.jar"]`라 JAVA_OPTS 환경변수를 자동으로 읽지 않음
- 해결: ENTRYPOINT를 `["sh", "-c", "java $JAVA_OPTS -jar app.jar"]`로 변경
- myWebsite는 기존 ENTRYPOINT에 `-Dspring.profiles.active=prod`가 있었으므로 JAVA_OPTS에 포함
- EC2의 compose 파일(~/retweet/compose.prod.yaml, ~/mywebsite/compose.prod.yaml)은 로컬과 달라서 EC2에서 직접 nano로 JAVA_OPTS 추가 후 force-recreate
- 확인 명령어: `docker exec <컨테이너> ps aux | grep java` → `-Xms128m -Xmx256m` 표시 확인

### GitHub Actions 배포 트러블슈팅
- 인스턴스 중지/유형변경/재시작 후 퍼블릭 IP 변경 → EC2_HOST secret 업데이트 필요
- SSH handshake failed: EOF → 메모리 부족으로 SSH 데몬도 응답 못한 상태였음
- command_timeout 미설정으로 30초 후 timeout → 3개 프로젝트 deploy.yml에 `command_timeout: 5m` 추가
- deploy.yml에 git pull 없이 이미지만 pull → EC2의 compose 파일이 업데이트 안 됨 (수동으로 EC2에서 git pull 필요)
- GHCR 403: docker login 필요 → `echo <TOKEN> | docker login ghcr.io -u mejisue --password-stdin`

### 3개 프로젝트 구조 파악
- sleact: compose.prod.yaml에 MySQL(shared-mysql) + Redis + backend, DB는 RDS 삭제 후 Docker MySQL로 전환
- retweet: ~/retweet/compose.prod.yaml (로컬의 backend/docker-compose.prod.yml과 다름)
- myWebsite: ~/mywebsite/compose.prod.yaml, DB_HOST=172.17.0.1로 sleact의 공유 MySQL 접근
- MySQL 1개에 DB 3개(sleact, threaleact, mywebsite) 운영 중
- 포트: retweet 8080, myWebsite 8082, sleact 8083

### 크롤링 자동화 (LaunchAgent) 수정
- 기존: 오전 9시 1회 StartCalendarInterval → 맥이 꺼져있으면 실행 안 됨
- 문제: runs=0, 한 번도 자동 실행된 적 없었음 (목요일 이후 크롤링 중단)
- 해결: plist를 매 30분 트리거로 변경 + morning_briefing.py에 중복 실행 방지 로직 추가
  - 오전 9시 이전이면 sys.exit(0)
  - 오늘 이미 실행됐으면 sys.exit(0) (saramin-YYYY-MM-DD.json 존재 여부로 판단)
- launchctl unload → load로 재적용 완료

## Q&A / 배운 것

### kswapd0란?
- 커널 스왑 데몬. 메모리 부족 시 디스크 스왑을 처리
- CPU 점유율이 높으면 메모리 부족 상태의 명확한 신호

### t3.small vs t3.medium
- t3.small: 2GB RAM, $0.0208/hr, 월 ~$15
- t3.medium: 4GB RAM, $0.0314/hr, 월 ~$22.6
- 포트폴리오용 Spring x3 + MySQL x1 구성은 t3.small 힙 제한 후 가능

### JAVA_OPTS
- Java 힙 제한 환경변수. `-Xms` 시작 힙, `-Xmx` 최대 힙
- Docker ENTRYPOINT가 exec form(`[]`)이면 환경변수 확장 안 됨 → shell form(`sh -c`)으로 변경 필요

### LaunchAgent vs LaunchDaemon
- LaunchAgent: 로그인 사용자 권한, ~/Library/LaunchAgents/
- LaunchDaemon: 시스템 권한, /Library/LaunchDaemons/
- StartCalendarInterval: 잠자기 중 놓치면 깨어날 때 1회 실행, 완전히 꺼진 상태면 실행 안 됨

### launchctl 주요 명령어
- `launchctl list | grep <이름>`: 상태 확인 (PID, 종료코드)
- `launchctl kickstart -p gui/$(id -u)/<label>`: 강제 실행
- `launchctl print gui/$(id -u)/<label>`: 상세 상태 (runs, last exit code 등)
- `launchctl unload / load <plist>`: 설정 재적용

### 탄력적 IP
- 인스턴스 재시작 시 퍼블릭 IP 변경됨 → 탄력적 IP 연결하면 고정
- 연결된 상태(인스턴스/로드밸런서/NAT)면 무료, 미연결 보유 시 $0.005/hr 과금

### 자소서 오리누 주식회사 검토
- 지원동기 / 향후 비전 / 관련 경력 3개 항목 각 1500byte 이내
- 주요 수정: "~이 아니라" 패턴 제거, CI/CD 파트 구체화(포트번호, 공유MySQL), 시제 정리

## 미해결
- [ ] deploy.yml에 git pull 미포함 → EC2 compose 파일 변경 시 수동 pull 필요 (자동화 개선 여지)
- [ ] 오리누 트래커 미등록 상태
