# 2026-06-03 — 컨텍스트 정리 #1

## 작업

### Next.js 프로젝트 초기 설정 (exam)
- 기존 Create Next App 기반 프로젝트
- Tailwind CSS v4 → 완전 제거, CSS 모듈 + postcss-custom-media 방식으로 전환
- postcss-import 설치해서 `@custom-media` 브레이크포인트를 공유 파일로 관리
- `app/styles/breakpoints.css`에 브레이크포인트 정의, 각 모듈에서 `@import`로 사용

### 컬러 시스템 설정
- `globals.css` `:root`에 CSS 변수로 색상 정의
- violet, slate, rose, lime, amber 계열
- Tailwind 제거 후 `@theme inline` 블록도 제거

### 반응형 브레이크포인트
- mobile: max-width 767px (기본값)
- tablet: 768px ~ 1024px (`--tablet`)
- desktop: 1025px 이상 (`--desktop`)
- `postcss-custom-media` v12에서 `importFrom` 옵션 제거됨 → `postcss-import`로 해결

### Header 컴포넌트
- `app/components/Header.tsx` + `Header.module.css`
- SVG 로고 → PNG로 변경 (small-logo.png, big-logo.png)
- 모바일: small-logo, tablet/desktop: big-logo (CSS `display` 분기)
- `Link href="/"` 로 홈 이동 연결
- `layout.tsx`에 Header 배치

### Plusbar 컴포넌트
- 입력창(80%) + 버튼(20%) flex 구조
- 버튼: isEmpty 여부 + 화면 크기로 4가지 이미지 분기
  - isEmpty + mobile → empty-plus-small.png
  - isEmpty + tablet/desktop → empty-plus.png
  - !isEmpty + mobile → plus-small.png
  - !isEmpty + tablet/desktop → plus.png
- JSX로 isEmpty 분기, CSS로 반응형 분기
- `'use client'` 컴포넌트, input useState, addItem API 호출
- isEmpty는 page.tsx에서 prop으로 받아 로컬 state로 관리
- router.refresh() 제거 (state 초기화 문제)

### CheckList 컴포넌트
- `app/components/CheckList.tsx` + `CheckList.module.css`
- items 전체를 page.tsx에서 prop으로 받음
- `isCompleted: false` → todoItems, `isCompleted: true` → doneItems
- `items.length === 0`일 때만 empty 이미지 표시 (doneItems.length 아님)
- desktop: flex row로 todo/done 나란히
- `CheckItem` 컴포넌트로 각 아이템 렌더링

### CheckItem 컴포넌트
- `'use client'`, isCompleted 로컬 state 관리
- 클릭 시 `updateItem(id, !isCompleted)` 호출 후 로컬 state 토글
- todo: white 배경 + 빈 원(FEFCE8), done: violet-100 배경 + violet-600 원 + 체크마크 SVG
- done일 때 text-decoration: line-through

### API (app/lib/api/item.ts)
- `getItems()`: GET /items
- `getItem(id)`: GET /items/{id}
- `addItem(name)`: POST /items, body: { name }
- `updateItem(id, isCompleted)`: getItem으로 현재 값 조회 후 PATCH (name, memo, imageUrl, isCompleted 전부 포함)
- BASE_URL 상수로 통일

## Q&A / 배운 것

### CSS 변수 (Custom Properties)
- `--` 접두사 필수 (CSS 표준 예약)
- `:root`에 선언하면 전역 사용 가능
- `var(--변수명)`으로 사용
- 이름 형식 자유, 관례는 `--color-`, `--spacing-` 등 접두사 방식 or semantic 방식

### SVG vs PNG
- 로고는 SVG가 기본 추천 (벡터, 색 변경 가능)
- 복잡하거나 이미 PNG로 준비된 경우 PNG 사용
- JSX에 직접 SVG 붙여넣으면 props로 크기/색 제어 가능

### Next.js Image fill prop
- 부모에 `position: relative` + 명시적 height 필수
- fill 없이 반응형: `width={0} height={0} sizes="100vw" style={{ width: '100%', height: 'auto' }}`

### Tailwind 모바일 퍼스트
- 기본값이 가장 작은 화면, 브레이크포인트는 "이상일 때" 적용
- `hidden md:block` = md 미만 숨김, md 이상 표시

### 반응형 실무 방식
- CSS 모듈 + 미디어쿼리: 대기업/디자인시스템
- Tailwind: 스타트업/빠른 개발
- CSS-in-JS: 컴포넌트에 스타일 완전히 묶고 싶을 때

### postcss-custom-media v12
- `importFrom` 옵션 제거됨
- 대안: `postcss-import` 설치 후 각 CSS 파일에서 `@import`

### Next.js 서버/클라이언트 컴포넌트
- async 서버 컴포넌트에서 직접 fetch 가능
- 인터랙션(useState, onClick) 필요하면 `'use client'` 필수
- `router.refresh()`로 서버 컴포넌트 재렌더 가능하나 client state 초기화 이슈 있음

## 미해결
- [ ] updateItem에서 memo/imageUrl null 처리 (`?? ''`) 미적용 상태
- [ ] CheckItem 토글 후 실제 API 성공 여부 확인 필요
- [ ] empty 이미지 크기/위치 CSS 미완성 (내가 완성 예정)
- [ ] Plusbar isEmpty 버튼 토글 문제 완전 해결 확인 필요
