# 2026-06-10 — 컨텍스트 정리 #5

## 작업

### 이력서(CV) 풀스택 버전 제작 — 직무별 토글 가능한 HTML 이력서
- 목표: 백엔드/풀스택/프론트엔드 모두 지원 예정. 기존 `myWebsite/front/public/cv.pdf`의 3개 프로젝트(Sleact, Retweet, MyWebsite) 소개를 BE/FE 어느 한쪽에 치우치지 않게, JD에 맞춰 강조점을 바꿀 수 있고 수치화된 데이터가 들어간 형태로 재작성
- 기존 cv.pdf(PDF, 5페이지) 구조를 Read로 분석: 헤더/정보카드 → 간략소개/핵심역량 → 기술스택/학력 → 경력/자격/포트폴리오 → 프로젝트 3개
- 작업 시작 전 모델을 Opus 4.8로 전환(`/model`)
- 의사결정 (AskUserQuestion):
  - 강조 방식: **직무 프리셋 토글** (버튼으로 BE/풀스택/FE 전환 시 핵심역량 문구 + 프로젝트 순서 + 불릿 순서 자동 재정렬)
  - 프로젝트 범위: 기존 3개(Sleact/Retweet/MyWebsite) + **AI 채용공고 자동화 파이프라인** 추가
- 코드/문서 검증 (Bash/Read 병렬 탐색):
  - `/Users/soo/Desktop/취업/` = AI 자동화 프로젝트 본체 (crawl.py, morning_briefing.py, .claude/skills/데일리-채용-브리핑 등)
  - MyWebsite 백엔드에 AdminController/AdminStats/MonthlyPostCount/TagCount 등 어드민 통계 API 실재 확인 → BE 불릿로 활용 가능
  - Retweet에 Sentry(FE+BE)·Amplitude 실재 확인, 백엔드 테스트 11개
- **사용자가 작업 중단시키고 출처 문서 지정**: `/Users/soo/Documents/경험-블록-인덱스.md`, `/Users/soo/Documents/경험-블록-통합.md`
  - 두 문서를 전부 읽음 (블록 A~AC, S, V, W = 총 27개 경험 블록, STAR + 어필포인트 + 직무태그 + 출처 정리된 정본)
  - 이후 모든 프로젝트 불릿을 이 블록 ID 기준으로 작성 (코드 직접 재탐색 대신 정본 활용)
- `/Users/soo/Desktop/cv/` 폴더 생성, 4개 파일 작성:
  - `data.js`: 모든 콘텐츠(프로필/태그라인/핵심역량/기술스택/경력/프로젝트 4개+불릿) — 직무별(fullstack/backend/frontend) 데이터 분리, 각 불릿에 tag(be/fe/infra/ai) + block(출처 블록ID) 부여
  - `style.css`: A4 인쇄 대응 + 화면용 토글바 스타일, 태그 색상 체계
  - `render.js`: CV_DATA → DOM 렌더링, 직무 프리셋별 프로젝트/불릿 재정렬 로직 (PRESETS 객체)
  - `index.html`: 토글바(풀스택/백엔드/프론트엔드 + PDF 인쇄 버튼) + 뼈대
  - `README.md`: 사용법, 파일별 역할, PDF 추출 방법
- Node로 데이터 무결성 검증: 전체 불릿 BE 11 / FE 11로 균형 확인, 직무별 정렬 로직(sortB) 정상 동작 확인
- 브라우저로 열어 확인 → headless Chrome으로 PDF 생성(`cv-fullstack.pdf`) → Read로 PDF 내용 직접 확인 (4페이지, 한글/태그/색상 정상)

### 토글 vs 단일 버전 논의
- 사용자 질문: "토글 가능한 게 단점 아닐까?"
- 답변 요지: 토글 자체는 작성 도구일 뿐 상대에게 안 보임. 진짜 리스크는 "BE/FE 둘 다 욱여넣어 포커스 흐려보임" → 토글로 직무별 단일 버전을 뽑으면 해소됨
- 사용자: "포트폴리오 사이트엔 1개 버전만 올려야 함" → **풀스택 버전을 대표로 채택** (AskUserQuestion). 토글 도구는 삭제하지 않고 유지(추후 직무별 PDF 추출용)

### 사용자 요청 수정사항 반영 (data.js / render.js)
- 기술스택 "언어"에서 Python 제거
- Sleact 소개에서 "클론"/"Slack" 언급 제거 → "워크스페이스·채널·다이렉트 메시지를 지원하는 기업용 실시간 협업 채팅 서비스. FE/BE/인프라 단독 구축."
- AI 자동화 프로젝트 stack을 `["Claude Code"]`만 남기고 나머지(Python/Playwright/Google Calendar API/Slack Webhook/launchd) 제거
- "일 400건+" → "일 평균 10건 이상"
- 사용자가 `/Users/soo/Desktop/cv/photo.JPG` 추가 → render.js의 photo-placeholder를 `<img class="photo" src="photo.JPG">`로 교체
- headless Chrome으로 PDF 재생성 (`--allow-file-access-from-files` 플래그 추가, 사진 포함되며 370KB)

### myWebsite 저장소 반영 (git)
- `/Users/soo/Documents/myWebsite`에서 브랜치 `chore/update-cv-pdf` 생성
- `/Users/soo/Desktop/cv/cv-fullstack.pdf` → `front/public/cv.pdf` 덮어쓰기 (754KB → 370KB)
- 커밋: `6bd9c61 docs: 이력서(cv.pdf) 풀스택 버전으로 갱신` (Co-Authored-By 없음, 한국어 메시지 — 메모리 규칙 준수)
- main으로 체크아웃 후 `git merge chore/update-cv-pdf` (Fast-forward) → main에 반영 완료
- **현재 상태: 로컬 main에만 반영, origin/main push는 아직 안 함**

## Q&A / 배운 것

### 경험 블록 통합 문서의 역할
- `/Users/soo/Documents/경험-블록-인덱스.md` + `경험-블록-통합.md`는 3개 프로젝트(Retweet/Sleact/myWebsite) + 현장경험 + AI자동화 + 디자인시스템에서 추출한 27개 STAR 경험 블록의 정본
- 각 블록에 출처(FE/BE/프로젝트), 핵심 키워드, 수치화된 성과, 직무 태그가 정리되어 있어 이력서/자소서 작성 시 코드 재탐색 없이 이 문서를 1차 소스로 사용해야 함

## 미해결
- [ ] `myWebsite` main 브랜치를 `origin/main`에 push 안 함 — 사이트 실제 배포 반영하려면 push 필요
- [ ] `chore/update-cv-pdf` 등 머지 완료된 작업 브랜치 정리(삭제) 안 함 (브랜치 목록에 다수 잔존 — 이번 작업과 무관한 기존 브랜치들도 다수)
- [ ] 사진(photo.JPG) 비율/잘림 등 최종 디자인 미세 조정 여부 미확인
