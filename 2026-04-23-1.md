# 2026-04-23 — 컨텍스트 정리 #1

## 작업

### morning_briefing.py — 오늘마감/내일마감 캘린더 미등록 버그 수정
- 문제: `parse_deadline_to_iso()`의 `skip_keywords`에 `오늘마감`이 포함되어 있어 블루비즈넷 등 오늘 마감 공고가 캘린더에 등록되지 않음
- 해결: `오늘마감` → 오늘 날짜(`datetime.now().strftime("%Y-%m-%d")`), `내일마감` → 내일 날짜로 변환하도록 수정
- 현재 상태: 수정 완료, 내일 크롤링부터 적용

### morning_briefing.py — 상시채용 캘린더 등록 기능 추가
- 문제: 상시채용 공고는 캘린더에 전혀 표시되지 않아 빼먹을 위험
- 해결 방향: 크롤링 당일 + 7일 후를 검토 리마인더 날짜로 등록
- 변경 내용:
  - `is_always_open()` 헬퍼 함수 추가
  - 상시채용 이벤트 제목: `[상시·검토] 회사 — 직무`
  - 색상: 청록(7) — 기존 빨강(적극지원)/노랑(조건부 추천)과 구분
  - `REVIEW_DAYS = 7` 상수로 관리
  - `save_calendar_events()`, `add_events_to_calendar()` 두 함수 모두 반영
- 현재 상태: 수정 완료, 오늘 상시채용 공고(원티드 7건 등)는 수동 등록 필요

### 아침 크롤링 결과 확인
- 크롤링 정상 실행 확인
- 지원-트래커.md: 오늘 공고 추가됨 (496~539줄, 사람인/잡코리아/자소설/원티드)
- calendar_events-2026-04-23.json 생성됨 (25개 이벤트)
- calendar_event_map.json에 오늘 공고 이벤트 ID 전부 등록 확인
- 상시채용/오늘마감은 기존 로직상 스킵됨 (이번에 버그 수정)

### 경험-블록-통합.md — 블록 B 수치 보강
- CloudFront 응답 시간 실측값 추가: cold 27ms / warm 25ms
- S3 직접 비교 불가 사유 명시 (퍼블릭 접근 차단, 보안 정책 의도)
- 보강 필요 항목(LCP, 월 비용) 제거 결정
  - LCP: 프론트엔드 지표라 백엔드/인프라 어필과 결이 다름
  - 월 비용: 처음부터 CloudFront 사용이라 비교 기준 없음, 개인 프로젝트 트래픽으로 수치가 미미할 것

## Q&A / 배운 것

### S3 vs CloudFront 관계
- S3: 특정 리전(ap-northeast-2 서울)에 존재하는 오브젝트 스토리지. 원본 저장소.
- CloudFront: 전 세계 450+ 엣지 서버에 캐시하는 CDN. 배달부 역할.
- 첫 요청(cold): CloudFront 엣지 → S3에서 가져와 캐시 후 반환
- 재요청(warm): CloudFront 엣지에서 바로 반환 (S3 접근 없음)
- 이 프로젝트: S3는 백엔드만 접근, 프론트엔드엔 CloudFront URL만 노출

### CloudFront 응답 시간 측정 방법
- 브라우저 Network 탭 → Img 필터 → Timing 탭 → TTFB 확인
- Response Header `X-Cache: Hit from cloudfront` = 캐시 히트
- `<img>` 태그 측정 스크립트: fetch()는 CORS 차단됨, img 태그는 CORS 검사 없음

### CORS 오류 원인
- `fetch()`는 cross-origin 요청 시 브라우저가 `Access-Control-Allow-Origin` 헤더 확인
- CloudFront가 해당 헤더 미설정 → localhost에서 fetch 차단
- `<img>` 태그 로딩은 CORS 검사 없으므로 우회 가능
- 프로덕션 도메인(retweet.mejisue.site)에서는 fetch도 가능

### PerformanceResourceTiming TTFB가 0ms로 나오는 이유
- cross-origin 리소스는 `Timing-Allow-Origin` 응답 헤더 없으면 responseStart = 0
- CloudFront가 해당 헤더 미설정이므로 TTFB 측정 불가
- 전체 응답 시간(total)은 img.onload 타이밍으로 측정 가능

### cold/warm 차이가 2ms로 거의 없는 이유
- 이미 이전 접근으로 서울 엣지 서버에 캐시된 상태
- 진짜 cold 측정하려면 CloudFront invalidation 필요

## 미해결
- [ ] 오늘 상시채용 공고(원티드 7건 등) 캘린더 수동 등록
- [ ] 블록 B 보강 필요 줄 최종 삭제 (사용자 확인 후 진행 예정)
