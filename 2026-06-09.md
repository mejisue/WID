# 📅 2026-06-09 — WID

## 🗂️ 환경 정리

### morning_briefing.py — crawl_results 제거
- `crawl_results/` 폴더가 플랫폼별 JSON 저장 + 중복 실행 방지 sentinel 두 역할을 하고 있었음
- sentinel을 `last_run.txt` 단일 파일로 교체 (오늘 날짜 한 줄 기록)
- JSON 저장 블록 제거, `crawl_results/` 폴더 삭제

### 취업 폴더 대청소 (`/Users/soo/Desktop/취업`)
- 삭제: `calendar_events-*.json` 8개, 디버그 파일, `briefings/`, `token.json`, `credentials.json`, 일회성 스크립트들
- 남긴 것: 크롤러 파일들, `morning_briefing.py`, `service_account.json`, `지원-트래커.md`

### WID 스킬 수정
- push 완료 후 로컬 파일 삭제하는 Step 추가

## 🚀 retweet — Sentry 프론트엔드 도입

### 작업 내용 (`feat/sentry-frontend`)
- `@sentry/react` + `@sentry/vite-plugin` 설치
- `src/sentry.ts` 생성: `Sentry.init()` + React Router v7 트레이싱 통합
- `main.tsx`: React 19 에러 핸들러 방식 적용 (`onUncaughtError` / `onCaughtError` / `onRecoverableError`)
- `api/client.ts`: 401 외 API 에러에 `captureException` 추가
- `root-route.tsx`: `Routes` → `SentryRoutes`(`withSentryReactRouterV7Routing`) 교체
- `vite.config.ts`: `sentryVitePlugin` + `sourcemap: true`
- Sentry 대시보드에서 에러 캡처 확인 후 커밋 완료

## 💡 배운 것 / Q&A

### 소스맵(Source Map)
- 빌드 시 난독화된 코드 ↔ 원본 코드 위치 대응표
- `@sentry/vite-plugin`이 빌드 시 Sentry 서버에 자동 업로드
- 사용자에게는 소스맵이 노출되지 않음

### Sentry DSN
- "Data Source Name" — 에러를 어느 Sentry 프로젝트로 보낼지 알려주는 주소
- Settings → Projects → Client Keys(DSN)에서 확인

### React 19 에러 핸들러 vs ErrorBoundary
- ErrorBoundary: React 18 방식, 렌더링 크래시만 캐치
- React 19: `createRoot` 옵션으로 더 낮은 레벨에서 캐치 (uncaught / caught / recoverable 구분)
- 공식 문서: `react.dev/reference/react-dom/client/createRoot`

### React Router v7 Sentry 통합
- `reactRouterV7BrowserTracingIntegration`만으론 부족, 라우팅 방식에 맞는 래핑 필요
- `<Routes>` 방식 → `withSentryReactRouterV7Routing(Routes)`으로 교체
- 공식 문서: `docs.sentry.io/platforms/javascript/guides/react/features/react-router/v7/`

## ✍️ 오늘 추가로 한 것

- 코드잇 프리코스 라우팅 파트 수강 완료

## 📌 미해결
- [ ] `feat/sentry-frontend` PR 작성
- [ ] retweet 백엔드 (Spring Boot) Sentry 적용
- [ ] `SENTRY_AUTH_TOKEN` CI/CD 환경변수 등록 (소스맵 업로드용)
