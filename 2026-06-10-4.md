# 2026-06-10 — 컨텍스트 정리 #4

## 작업

### 구글 폰트(Nanum Gothic) 적용 — myWebsite (front)
- `front/app/layout.tsx`에 `next/font/google`의 `Nanum_Gothic` 추가 (weight: 700, 800), `--font-nanum-gothic` CSS 변수로 등록
- `front/app/globals.css`의 `@theme inline`에서 `--font-sans: var(--font-geist-sans)` → `var(--font-nanum-gothic)`로 변경
- `<html lang="en">` → `<html lang="ko")`로 변경 (한국어 콘텐츠 비중 큼 → 접근성/SEO 위해)
- 문제: `--font-sans`만 바꿨는데 적용이 안 됨
  - 원인: next/font의 `.variable` 클래스가 `body`에 정의되는데, Tailwind preflight의 `--default-font-family`는 `:root`(html) 스코프에서 `var(--font-sans)`를 참조 → CSS 변수는 부모→자식 상속만 되므로 html에서 `--font-nanum-gothic`을 못 찾아 invalid 처리됨
  - 해결: `@layer base`의 `body` 셀렉터에 `font-sans` 유틸리티 직접 추가 (`@apply bg-background text-foreground font-sans;`) → body 자기 스코프에서 변수 정상 해석
- 추가 문제: 수정 후에도 적용 안 됨 → dev 서버(Turbopack) HMR 캐시 문제로 판단, `.next` 삭제 후 `pnpm dev` 재시작하여 정상 컴파일 확인 (`body { font-family: var(--font-nanum-gothic) }` 생성됨)
- 중간에 `Dongle` 폰트(변수명은 `nanumMyeongjo`로 오기재)로 잠깐 바꿨다가 "글자가 너무 작다"는 이슈 있었음 → Dongle은 x-height가 작아 같은 font-size에도 작아 보이는 폰트라는 점 안내. 최종적으로 `Nanum_Gothic`(weight 700/800)으로 확정
- 커밋: `2187e6c style: 기본 폰트를 Nanum Gothic으로 변경, html lang ko 설정`
- 브랜치 `style-font`(origin/main 추적) → 로컬 `main` 브랜치를 `origin/main` 기준으로 새로 생성하고 merge 완료
- 현재 상태: 로컬 main과 origin/main 동일 (이미 push 반영 확인됨, working tree clean)

## Q&A / 배운 것

### next/font/google에 특정 폰트가 있는지 / 정확한 export 이름 확인하는 법
- `node_modules/next/dist/compiled/@next/font/dist/google/index.d.ts`에 전체 export 함수 목록 존재
- `grep -oP "export declare function \K\w+" index.d.ts`로 전체 폰트 리스트(1907개) 추출 가능 → 이름 일부만 알아도 `grep -i 키워드`로 검색
- fonts.google.com은 디자인 미리보기/weight 확인용, 실제 코드에서 쓸 import 이름·지원 여부는 node_modules에서 확인해야 정확함

### Nanum Gothic subsets / 한글 지원 여부
- `Nanum_Gothic`의 `subsets` 옵션은 `'latin'`만 지원 (`korean` 옵션 없음)
- 하지만 subsets는 preload 서브셋 지정용일 뿐, 폰트 파일 자체에 한글 글리프 포함되어 있어 `latin`으로 둬도 한글 정상 렌더링됨

### `@theme inline` / `--font-sans` / Tailwind v4 폰트 적용 메커니즘
- `@theme inline`은 CSS 변수를 Tailwind 디자인 토큰으로 등록하는 Tailwind v4 문법
- Tailwind v4 preflight: `html { font-family: var(--default-font-family, ...) }`, `--default-font-family: var(--font-sans)` (`:root`에 정의)
- next/font의 `.variable` 클래스가 `body`에 있으면 `:root`(html)에서는 그 변수를 못 보므로, `body`에 `font-sans` 유틸을 직접 적용해야 함

### html lang 속성과 폰트의 관계
- `lang` 속성은 폰트 렌더링과 무관한 메타데이터 (스크린리더 발음, SEO, 브라우저 번역 기능에 영향)
- 한국어 콘텐츠 비중이 크면 `lang="ko"`로 설정하는 게 맞음

## 미해결
- (없음 — 작업 완료, push 반영 확인됨)
