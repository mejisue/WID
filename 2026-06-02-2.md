# 2026-06-02 — 컨텍스트 정리 #2

## 작업

### Sleact 포트폴리오 모달 리디자인 (feat/sleact-modal-redesign)

#### 타입 확장 — ProjectModal.tsx

```ts
export type DesignPoint = {
  title: string;
  problem?: string;
  solution: string;
  result?: string;
  video?: string;
};

export type TroubleshootingItem = {
  title: string;
  problem: string;
  solution: string;
  video?: string;
};

export type ProjectDetail = {
  description: string;
  motivation?: string;
  techStack: string[];
  features: string[];
  designPoints?: DesignPoint[];
  troubleshooting?: TroubleshootingItem[];
  links: { site?: string; github?: string; };
  image?: string;
  demoGif?: string;
};
```

#### 모달 레이아웃

- 크기: `max-w-lg` → `max-w-5xl w-[92vw]` (hasRichContent 조건부)
- hasRichContent 조건: `designPoints || troubleshooting || motivation || demoGif`
- 섹션 순서: demoGif(hero) → 왜 만들었는가 → Tech Stack → 핵심 설계 포인트 → 구현 기능 → 트러블슈팅 → Links
- 핵심 설계 포인트: 배지(문제/해결/결과) → 서술형 텍스트 (트러블슈팅처럼 보인다는 피드백)

#### 스크롤바 border-radius 버그 수정

- 원인: `overflow-y-auto`를 rounded 컨테이너 자체에 적용 → 스크롤바가 오른쪽 radius 덮어 직각처럼 보임
- 해결: 외부 `overflow-hidden p-0` + 내부 래퍼 `overflow-y-auto max-h-[92vh]`

```tsx
<DialogContent className="... rounded-2xl overflow-hidden p-0">
  <div className="overflow-y-auto max-h-[92vh] p-6 flex flex-col gap-4">
    ...
  </div>
</DialogContent>
```

#### 영상 배치 (demoGif + video 필드)

- `chat.webm` → `demoGif`, description 아래 hero (full-width)
- `mention.webm` → "채팅 UX 디테일" 설계 포인트 카드 하단 (full-width)
- `scroll-restore.webm` → "무한 스크롤 스크롤 튐 현상" 트러블슈팅 카드 하단 (full-width)

#### 리팩토링 (코드 리뷰 후)

- `DESIGN_POINT_ACCENTS` 배열: map 내부 → 모듈 스코프 호이스트, dead `badge` 프로퍼티 제거
- `GITHUB_ICON` SVG: 인라인 → 모듈 상수 추출
- `gif` 필드명 → `video` (WebM 전환 후 네이밍 정합성)

---

### GIF → WebM 변환 (화질 개선)

- 문제: MOV → GIF → WebM 이중 변환 시 GIF 256색 손실이 WebM에도 유지됨
- 해결: MOV 원본에서 직접 WebM VP9 변환

```bash
# CRF 24 (최종)
ffmpeg -y -i "sleact영상.mov" -c:v libvpx-vp9 -b:v 0 -crf 24 -loop 0 -an -pix_fmt yuv420p "chat.webm"
ffmpeg -y -i "멘션.mov" -c:v libvpx-vp9 -b:v 0 -crf 24 -loop 0 -an -pix_fmt yuv420p "mention.webm"
ffmpeg -y -i "스크롤복원.mov" -c:v libvpx-vp9 -b:v 0 -crf 24 -loop 0 -an -pix_fmt yuv420p "scroll-restore.webm"
```

| 파일 | GIF | CRF 18 | CRF 24 (최종) |
|---|---|---|---|
| 채팅 | 153K | 851K | 749K |
| 멘션 | 102K | 336K | 271K |
| 스크롤복원 | 179K | 445K | 368K |

- `<img>` → `<video autoPlay loop muted playsInline>` 교체
- `.gif` 파일 삭제

---

### 한글 파일명 → ASCII 변경 (버그 수정)

- 문제: 모달에서 `채팅.webm` hero 영상이 표시 안 됨
- 원인: 브라우저가 한글 URL 인코딩 처리 시 리소스 못 찾음

```bash
mv 채팅.webm chat.webm
mv 멘션.webm mention.webm
mv 스크롤복원.webm scroll-restore.webm
```

---

### PostEditor.tsx 버그 수정 (코드 리뷰 후)

```ts
// 1. 이미지 업로드 실패 시 orphan 개행 제거
setContent((prev) => prev.replace(placeholder, '').replace(/\n{3,}/g, '\n\n'));

// 2. Tab 체크를 함수 최상단으로 이동 (모든 키입력마다 불필요한 DOM 접근 방지)
if (e.key !== 'Tab') return;
const textarea = textareaRef.current;
...

// 3. 체크박스 regex 대문자 X 누락 수정
/- \[[ xX]\]/g
```

---

## Q&A / 배운 것

### CRF (Constant Rate Factor)
- 비트레이트 고정이 아닌 품질 고정 방식 인코딩
- 낮을수록 고화질/큰 용량, 높을수록 저화질/작은 용량
- VP9 웹 스트리밍 적정선: CRF 24~28

### 비디오 최적화 방법
- 해상도 제한: `-vf scale=1280:-2`
- CRF 조정 (18 → 24~28)
- 2-pass 인코딩: 같은 용량에서 화질 향상
- AV1 코덱: VP9 대비 30~50% 용량 절감 (인코딩 시간↑)
- `preload="metadata"` 속성으로 초기 로딩 최적화

### overflow + border-radius 스크롤바 버그 패턴
- `overflow-y-auto`가 rounded 컨테이너에 직접 붙으면 스크롤바가 radius 덮음
- 패턴: 외부 `overflow-hidden` + 내부 래퍼 `overflow-y-auto`

### GIF vs WebM 화질
- GIF: 프레임당 256색 제한
- MOV→GIF→WebM 이중 변환 시 GIF 손실이 고정됨 → MOV→WebM 직변환 필수
- 로컬 = 프로덕션 화질 (Vercel은 바이너리 파일 재인코딩 없음)

### 정적 파일 한글 파일명 주의
- Next.js public 디렉토리에서 한글 파일명 사용 시 브라우저 URL 인코딩 문제 발생 가능
- 정적 미디어 파일은 ASCII 파일명 사용 권장

## 미해결
- [ ] 이미지 다중 선택 시 insertAt 위치 stale 이슈 (실사용 영향 없음)
- [ ] GITHUB_ICON SVG가 HomePage.tsx nav에도 중복 존재 → 공용 컴포넌트 추출 검토
