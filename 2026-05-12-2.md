# 2026-05-12 — 컨텍스트 정리 #2

## 작업

### morning_briefing.py Google Calendar 인증 방식 전환 (OAuth → Service Account)

- **문제**: `invalid_grant: Token has been expired or revoked` 에러가 2주 동안 반복 발생
  - launchd로 자동 실행되는 스크립트가 토큰 만료 시 재인증 불가 (브라우저 열 수 없음)
  - 에러 로그: `/Users/soo/Desktop/취업/briefings/launchd-error.log`
  - launchctl 확인 시 exit code 78 (조기 실패)
  - 마지막 정상 실행: 2026-05-04

- **초기 진단 오류**: OAuth consent screen 테스트 모드(7일 만료)를 원인으로 추정 → 사용자가 프로덕션 단계임을 확인하며 기각

- **실제 원인**: OAuth user token 구조적 한계
  - Google 계정 보안 이벤트 등으로 refresh token 자체가 주기적으로 무효화됨
  - 자동화 스크립트에 OAuth user 인증은 부적합

- **해결**: Service Account 방식으로 전환
  - GCP Console에서 Service Account 생성 (`me-279@mindful-genius-493904-p8.iam.gserviceaccount.com`)
  - JSON 키 다운로드 → `/Users/soo/Desktop/취업/service_account.json`으로 이동
  - Google Calendar에서 "지수" 캘린더를 service account 이메일에 편집 권한 공유
  - `morning_briefing.py` 코드 수정:
    - import: `google.oauth2.credentials`, `InstalledAppFlow`, `Request` → `google.oauth2.service_account`
    - `get_calendar_service()` 함수: OAuth flow 제거, `service_account.Credentials.from_service_account_file()` 사용
    - `CREDS_FILE`, `TOKEN_FILE`, `HEADLESS` 변수 제거 → `SA_FILE`, `CALENDAR_ID` 추가
    - `calendarId="primary"` 3곳 → `calendarId=CALENDAR_ID` 교체
    - `CALENDAR_ID = "februstar11@gmail.com"`

- **테스트 결과**: 인증 성공 + 캘린더 접근 성공 확인

- **현재 상태**: 완료. 내일 아침 Mac 켤 때 정상 실행 예정

## Q&A / 배운 것

### OAuth user token vs Service Account 차이
- OAuth user token: 사람이 직접 허용, refresh token 주기적 만료 가능, 재인증 시 사람 개입 필요
- Service Account: JSON 키 파일로 봇 인증, 만료 없음, 완전 자동화 가능
- 자동화 스크립트에는 Service Account가 적합

### launchd RunAtLoad 동작
- `RunAtLoad: true` = 로그인 시 1회 실행 (의도된 동작, 수정 불필요)
- `StartCalendarInterval` 없으면 매일 자동 실행 안 됨 — 이 스크립트는 로그인 시 1회가 의도

### Service Account 캘린더 접근
- Service Account는 자체 빈 캘린더를 가짐
- 본인 캘린더에 이벤트를 쓰려면 캘린더 공유 설정 필수 (편집 권한)
- `calendarId="primary"` 는 service account 자신의 캘린더 → 반드시 실제 캘린더 ID로 교체 필요

### GCP Service Account 생성 시 권한 설정
- "권한 부여(IAM 역할)"와 "액세스 권한이 있는 주 구성원" 모두 건너뛰어도 됨
- Calendar API 접근은 IAM이 아닌 캘린더 공유 설정으로 제어

## 미해결
- 없음
