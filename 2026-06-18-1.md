# 2026-06-18 — 컨텍스트 정리 #1

## 작업

### revalidateTag 캐시 무효화 → 서버 액션으로 전환
- 프로젝트(myWebsite front)에서 `revalidateTag('posts', 'max')` 사용 중. 동작 이해 + 개선.
- 기존 구조: 클라이언트 `posts.ts`의 mutation 함수(createPost/updatePost/deletePost)가 Spring 백엔드 직접 호출 → 별도로 `fetch('/api/revalidate')` 라우트 핸들러 호출 → 라우트가 `revalidateTag('posts', 'max')` 실행. 라우트에서 `getServerSession`으로 어드민 권한 체크.
- 코드 주석 오류 발견: `// 두 번째 인자 'max' = 즉시 만료` → 틀림. `'max'`는 즉시 만료 아니라 stale-while-revalidate(낡은 화면 먼저 보여주고 백그라운드 갱신).
- 변경 내용:
  - 신규 `front/lib/actions/posts.ts` 생성 — `'use server'` 서버 액션 `revalidatePostCache(id?)`. auth 체크 후 `updateTag('posts')` + `updateTag('post-${id}')`.
  - `front/lib/api/posts.ts` 수정 — 내부 `revalidatePosts` 헬퍼가 `fetch('/api/revalidate')` 대신 서버 액션 `revalidatePostCache` 직접 호출. try/catch 유지(실패해도 글 작업은 성공 처리, 최대 1시간 뒤 자동 갱신).
  - `front/app/api/revalidate/route.ts` 라우트 핸들러 삭제(`rm -rf front/app/api/revalidate`).
- `updateTag` 선택 이유: 어드민이 글 수정 후 새로고침하면 본인 수정 즉시 반영돼야 함. `'max'`는 첫 방문 때 옛날 글 한 번 더 보임 → 어드민용으로 부적합.
- 현재 상태: `npx tsc --noEmit` 통과. 남은 에러 1개는 `.next/types/validator.ts`가 삭제된 라우트 참조하는 stale 캐시 → 다음 `next dev`/`build` 때 자동 재생성되며 사라짐. 내 코드 문제 아님.
- 미확인: create/update/delete는 여전히 브라우저에서 Spring 직접 호출(그대로 둠). 이번엔 캐시 무효화 경로만 서버 액션으로 전환.

## Q&A / 배운 것

### revalidateTag 동작 원리
- Next.js는 fetch 결과를 태그(이름표) 붙여 캐시. `next: { tags: ['posts'] }`로 태그 부여.
- `revalidateTag('posts')` = 그 태그 붙은 캐시 전부 무효화 → 다음 요청 때 새로 가져옴.
- 태그 이름은 fetch 쪽과 정확히 일치해야 함(오타 시 무효화 안 됨).
- `cache: 'no-store'`는 애초에 캐시 안 하므로 태그 못 씀(`getPostsPage`가 그래서 태그 없음).

### 'max' 두 번째 인자 진짜 의미 (Next 16)
- `revalidateTag(tag, profile)` — 두 번째 인자는 Next 16부터 필수(없으면 deprecated 경고).
- `'max'` = stale-while-revalidate. "낡음" 표시만 하고, 그 태그 쓰는 페이지 다음 방문 때 낡은 화면 먼저 + 백그라운드 갱신. 즉시 아님.
- `{ expire: 0 }` = 진짜 즉시 만료(다음 요청 blocking). 웹훅/외부 서비스용.
- `updateTag(tag)` = 같은 요청에서 바로 최신 데이터. 서버 액션용. Next 공식 추천(즉시 반영 필요 시).
- 출처: https://nextjs.org/docs/app/api-reference/functions/revalidateTag

### 서버 액션 vs 라우트 핸들러
- 서버 액션: `'use server'` 함수. 클라이언트에서 함수처럼 호출 → Next가 서버 실행 + 네트워크 배관 자동 처리. fetch URL/JSON 파싱/NextResponse 불필요.
- 둘 다 "클라이언트가 서버 코드 호출하는 통로" 역할 → 중복. 서버 액션 쓰면 라우트 핸들러 제거 가능.
- `updateTag`는 서버 액션/라우트 핸들러 안에서만 호출 가능.

## 미해결
- [ ] `npm run dev`로 글 작성/수정/삭제 후 화면 즉시 갱신 실제 확인
- [ ] create/update/delete 자체를 서버 액션으로 옮길지 여부(현재는 캐시 무효화만 전환)
