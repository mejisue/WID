# 2026-04-28 — 컨텍스트 정리 #1

## 작업

### CLS 개선 (fix/cls-layout-shift → PR #22 → develop)
- 문제: CLS 0.133 (기준 0.1 초과)
- 원인: `GlobalLayout`에서 세션 로드 후 `ProfileButton`이 뒤늦게 렌더링되면서 헤더 레이아웃이 밀림 → main, footer까지 연쇄 shift 발생
- 해결: `useIsSessionLoaded()`로 세션 로드 여부 확인 후, 로드 전에는 `h-8 w-8 rounded-full bg-muted` skeleton으로 자리 예약
- 결과: CLS 0 (Slow 3G 환경 기준)
- PR: https://github.com/mejisue/retweet/pull/22 (base: develop)

### 브랜치 전략 정리
- 현재 프로젝트 상태: main이 develop보다 앞서 있음 (배포 관련 chore 커밋이 develop을 거치지 않고 main에 직접 들어감)
- 정상 Gitflow: 모든 작업(기능 + 배포설정)은 develop에서 분기 → develop PR → main PR
- hotfix만 예외적으로 main에서 분기 (운영 장애 + develop에 미완성 코드가 섞여있을 때)
- 현재는 main에서 분기하는 게 현실적 선택 (develop이 뒤처져 있으므로)

### 백엔드 application.yaml 의도치 않은 변경
- `frontend-url` 값에서 포트번호(5173)가 잘린 상태로 변경됨
- stash 없이 프론트 파일만 선택적으로 커밋하고 PR 진행
- 백엔드 yaml은 미복구 상태 (uncommitted changes로 남아있음)

## Q&A / 배운 것

### useReducer 사용 시점
- useState가 3~4개 이상 서로 연관될 때
- 여러 상태를 동시에 바꿔야 하는 상황이 자주 생길 때
- 현업에서는 Zustand, Redux Toolkit이 대체하는 경우 많아 직접 쓰는 빈도는 낮음

### 성능 최적화 판단 기준
- "느리다"는 주관이 아닌 Core Web Vitals 숫자로 판단
  - LCP < 2.5s, INP < 200ms, CLS < 0.1
- 측정 도구: Lighthouse, Chrome Performance 탭, Web Vitals 확장 프로그램
- 트래픽 없이 측정: CPU throttling(4x), Network Slow 3G 활용
- 순서: 측정 → 병목 특정 → 해결 (예방적 memo/useCallback은 지양)

### INP 측정
- `PerformanceObserver`로 `type: 'event'` 관찰
- click / keydown / pointerdown만 INP 대상 (hover 이벤트는 제외)
- `window.confirm()`은 동기 블로킹이라 Violation 발생 → 실제 성능 문제 아님
- 이 프로젝트 INP: 16ms (처리시간 0.1ms) → 정상

### LCP 측정 결과
- 132ms, url="" → LCP 요소가 텍스트 (이미지 아님) → 매우 빠름

### CLS 원인 분석 방법
- Chrome Performance 탭 → Layout Shift 항목 확인
- Web Vitals 확장 프로그램 → CLS 클릭 시 shift된 요소 하이라이트

### CORS
- 브라우저 보안 정책: 출처가 다른 서버 요청 시 서버가 명시적 허용 필요
- `pnpm preview`는 production 빌드 결과물 서빙 → .env.production URL 사용
- Vite env 우선순위: `.env.[mode]` > `.env.local` > `.env`
- CLS 측정은 dev 서버 + Slow 3G로 대체

### git reset --hard
- 브랜치 포인터를 강제로 다른 커밋으로 이동
- `--hard`: HEAD + 작업 디렉토리 파일까지 전부 맞춤
- `--soft`: HEAD만 이동, 파일은 staged 상태 유지
- `--mixed`: HEAD 이동 + staged 취소, 파일은 그대로

### Gitflow 브랜치 전략
- develop에서 모든 작업 분기 (기능, 버그, 성능, 배포설정 포함)
- main은 merge만 받음 (직접 커밋 금지, hotfix 예외)
- hotfix 조건: 운영 장애 + develop 즉시 배포 불가 상태일 때만

### git push --force
- 로컬과 원격 히스토리가 갈라졌을 때 원격을 로컬 기준으로 덮어씀
- 단순히 "로컬이 앞서 있다"가 아닌 "히스토리가 다르다"는 상황에서 필요

## 미해결
- [ ] backend/src/main/resources/application.yaml 의도치 않은 변경 복구
- [ ] develop 브랜치를 main 기준으로 reset --hard 후 정상화 (혼자 작업이므로 언제든 가능)
- [ ] DeletePostButton의 window.confirm() → AlertModal로 교체 (댓글 삭제와 UI 통일)
