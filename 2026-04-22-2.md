# 2026-04-22 — 컨텍스트 정리 #2

## 작업

### Sleact 블록 D — k6 WebSocket 부하 테스트 세팅 및 실행

- 목표: WebSocket/STOMP 기반 채팅 서버의 메시지 지연 시간 및 동시 접속 처리량 측정
- 프로젝트 위치: `/Users/soo/Documents/sleact`
- 백엔드: Spring Boot, STOMP over SockJS, Redis Pub/Sub
- 테스트 도구: k6

#### k6 스크립트 작성 (`k6/websocket-test.js`)
- SockJS WebSocket URL 형식: `ws://localhost:8080/connect/{server}/{session}/websocket?workspace=1`
- 인증: 로그인 API(`POST /api/users/login`) → `token` 쿠키 → SockJS 핸드셰이크에 전달
- STOMP 흐름: CONNECT → CONNECTED → SUBSCRIBE → SEND → MESSAGE(에코) 수신 → 지연 측정
- 커스텀 메트릭: `msg_latency_ms`, `connect_success`, `msg_sent`, `msg_received`

#### 삽질 1 — connect_success 50%
- 원인: `socket.on("open")`에서 `add(true)`, 이후 `res.status !== 101` 체크에서 `add(false)` 중복 호출
- 해결: `connectionCounted` 플래그로 한 번만 기록하도록 수정

#### 삽질 2 — msg_latency = 0 (STOMP CONNECTED 못 받음)
- 원인 1: WebSocket `open` 이벤트 즉시 STOMP CONNECT 전송 → SockJS `o` 프레임 도착 전이라 서버가 처리 못함
- 원인 2: k6에서 `sleep()` 대신 `socket.setTimeout()` 을 써야 이벤트가 올바르게 처리됨
- 해결:
  - `socket.on("open")`과 SockJS `o` 프레임 수신 양쪽에서 STOMP CONNECT 시도 (중복 방지 플래그)
  - `sleep()` → `socket.setTimeout()`으로 교체

#### 삽질 3 — SEND는 성공하는데 MESSAGE 에코 안 옴
- redis-cli로 확인: Redis publish는 정상 (`/topic/chat/channel/일반`에 메시지 도착)
- 원인: `RedisPubSubService.onMessage()`에 로그가 없어서 어디서 막히는지 불명확
- 임시 로그 추가 후 재시작 → 정상 동작 확인 (로그 후 제거)
- 실제 원인 추정: 백엔드 재시작 없이 Redis 구독 리스너가 제대로 등록되지 않은 상태였던 것으로 보임

#### 최종 테스트 결과

| 지표 | 1 VU | 50 VU 동시접속 |
|---|---|---|
| avg latency | 57ms | 12.52ms |
| p95 latency | 118ms | 20ms |
| connect_success | 100% | 100% |
| msg_sent | 4 | 314 |
| msg_received | 4 | 314 |
| 메시지 유실 | 0 | 0 |
| WS 연결 시간 avg | 9.84ms | 2.62ms |

- `경험-블록-통합.md` 블록 D 결과 테이블 마크다운으로 수정

## Q&A / 배운 것

### `./gradlew bootRun` vs IDE 실행 차이
- IDE Run Configuration에 환경변수(DB_HOST, JWT_SECRET_KEY 등)가 설정되어 있어서 IDE 실행은 성공
- 터미널 실행은 환경변수 없어서 Spring이 값을 못 찾고 실패
- 해결: 터미널에서 `export $(cat .env | xargs) && ./gradlew bootRun` 또는 IDE 실행 유지

### VU (Virtual User)
- 1 VU = 사용자 1명이 접속해서 메시지 보내는 시뮬레이션
- 50 VU = 50명 동시 접속 시뮬레이션
- `ramping-vus` executor로 단계적 증가(0→10→50→0)

### SockJS WebSocket 전송 프로토콜
- SockJS는 WebSocket 위에 자체 프레임 포맷 사용: `o`(open), `h`(heartbeat), `a["..."]`(message array), `c`(close)
- STOMP 프레임은 SockJS `a[...]` 배열 안에 JSON 문자열로 래핑됨
- k6에서 테스트 시 일반 WebSocket 스크립트로는 동작하지 않고 SockJS 파싱 로직 직접 구현 필요

### k6 WebSocket 이벤트 모델
- `socket.on("open")` 이벤트는 `sleep()` 이후에 뒤늦게 처리될 수 있음
- `sleep()` 내부에서 이벤트 처리 타이밍이 불안정
- `socket.setTimeout(callback, ms)` 을 쓰면 이벤트 큐에 정확히 동작

## 미해결
- [ ] 임시 로그 추가 후 정상 동작한 정확한 원인 불명확 (Redis 구독 리스너 미등록 추정)
- [ ] 로컬 환경 기준 수치라 프로덕션 환경과 차이 있을 수 있음
- [ ] 블록 D 결과를 자소서/포트폴리오에 어떻게 서술할지 미정
