# 2026-04-22 — 컨텍스트 정리 #1

## 작업

### AWS ALB 헬스체크 unhealthy 해결
- LB 새로 만든 후 타겟 그룹 `mejisue-target-ec2` unhealthy 상태
- 에러: `Health checks failed with these codes: [404]`
- EC2에서 `curl -I http://localhost:80/` → nginx 살아있고 Spring Boot 앱 응답 중, `/` 경로가 404 반환
- `/actuator/health` 도 404 (Spring Actuator 미설정)
- 삽질.md 확인 → 동일 케이스 기록 있었음 (ALB target unhealthy 항목)
- 해결: 대상 그룹 → Health checks → Success codes `200` → `200-404` 로 변경
- 현재 상태: healthy 전환 확인 중

### WID 자동화 시스템 구축
- `/정리` 스킬 생성: 대화 컨텍스트를 `YYYY-MM-DD-N.md` 로 저장, git push
- GitHub private repo `mejisue/WID` 생성 → public으로 전환 (스케줄러 접근 문제로)
- `/Users/soo/Documents/WID` git 초기화 및 remote 연결
- 스케줄러 생성: 매일 23:59 KST (14:59 UTC) 에 오늘 번호 파일들 읽어서 블로그용 `YYYY-MM-DD-Summary.md` 생성 후 push
- 트리거 ID: `trig_01MmY4QycEnejk8idxqHXixw`
- 테스트 실행 완료

## Q&A / 배운 것

### k6란?
- 부하 테스트 도구. 가상 사용자(VU) N명이 동시에 서버를 사용하는 상황 시뮬레이션
- `k6 run --vus 50 --duration 30s websocket-test.js` = 50명이 동시에 30초간 WebSocket 접속

### 부하 테스트 용어
| 용어 | 의미 |
|------|------|
| VU (Virtual Users) | 동시 접속 가상 사용자 수 |
| latency | 요청 → 응답까지 걸린 시간 (ms) |
| avg latency | 평균 지연 시간 |
| p95 latency | 100개 요청 중 95번째로 느린 요청의 지연 시간 |
| throughput | 초당 처리한 요청/메시지 수 |
| 메시지 유실 | 보낸 메시지가 응답 없이 누락되는 현상 |

### Claude Code 스케줄러 (Remote Trigger)
- Anthropic 클라우드에서 실행 → 로컬 파일 접근 불가
- git_repository 소스로 public repo만 접근 가능 (private은 GitHub OAuth로도 안 됨)
- MCP 커넥터는 별도 UUID 필요 (GitHub 연동은 OAuth라 UUID 없음)

## 미해결
- [ ] ALB 헬스체크 healthy 전환 최종 확인
- [ ] EC2 보안그룹 인바운드 규칙 확인 (HTTP 80)
- [ ] [retweet] S3 DeleteObject IAM explicit deny 정책 제거
