# 2026-04-22 — 컨텍스트 정리 #3

## 작업

### myWebsite 프론트 성능 테스트 및 LCP 개선

- **목표**: Lighthouse로 성능 측정 후 before/after 비교 (포트폴리오용)
- **측정 환경**: Chrome 시크릿 창, `pnpm build && pnpm start` (프로덕션 빌드)
- **측정 페이지**: `/post/13` (이미지 포함된 게시글 상세 페이지)

**Before (개선 전)**
- LCP: 0.7s / FCP: 0.3s / Performance: 100

**After (개선 후)**
- LCP: 0.6s / FCP: 0.3s / Performance: 100
- 이미지 크기: 469KB → ~177KB (62% 감소, WebP 자동 변환)

**변경 내용** (`front/app/post/[id]/page.tsx`)
- `<img>` 태그 → `next/image` 교체
- `priority` 속성 추가 (LCP 이미지 우선 로딩)
- 마크다운 첫 번째 이미지 URL 파싱 후 `react-dom preload`로 조기 요청
- ESLint `no-img-element` 경고 제거

### 브랜치 작업 삽질

- **문제 1**: `refactor/change-works-urls` 브랜치에서 작업 시작 → `develop` 브랜치가 `origin/main`보다 1커밋 뒤처져 있었음
- **문제 2**: `develop` pull 후 `pnpm install`이 `package.json` 기준으로 동기화하면서 `react-markdown`, `rehype-raw`, `remark-gfm` 제거됨 → 빌드 에러
- **해결**: local `develop`을 `origin/main`으로 reset 후 `perf/next-image-optimization` 브랜치 새로 생성

```bash
git stash
git checkout develop
git reset --hard origin/main
git checkout -b perf/next-image-optimization
git stash pop
```

### PR 및 배포

- PR #31: `perf/next-image-optimization` → 처음에 `develop`으로 보냈다가 `main`으로 수정 후 머지
- Vercel 배포 에러 발생

### Vercel 배포 에러 해결

- **에러**: `No Next.js version detected`
- **원인**: Vercel이 루트(`/`)의 `package.json`을 바라봤는데 Next.js가 `front/` 안에 있음. 루트에 `package-lock.json`(npm)이 있어서 pnpm → npm으로 패키지 매니저도 혼동
- **해결**: Vercel 대시보드 → Settings → General → Root Directory → `front` 설정
- **확인**: 웹에서 직접 재배포 후 성공 확인

---

## Q&A / 배운 것

### LCP / FCP / Performance 점수란

- **LCP (Largest Contentful Paint)**: 페이지에서 가장 큰 콘텐츠가 화면에 그려지는 시간. 좋음 기준 2.5s 이하
- **FCP (First Contentful Paint)**: 첫 번째 콘텐츠가 화면에 보이는 시간. 좋음 기준 1.8s 이하
- **Performance 점수**: LCP, FCP, TBT, CLS, Speed Index 가중 평균 0~100점

### dev 서버 vs 프로덕션 빌드

- dev 서버(`pnpm dev`)에서 Lighthouse 측정하면 점수가 왜곡됨
- 반드시 `pnpm build && pnpm start` 후 측정해야 실제 배포 환경과 동일

### `fetchpriority=high` Lighthouse 경고가 안 사라지는 이유

- `next/image`는 이미지를 `/_next/image?url=...` 형태로 최적화해서 서빙
- preload는 원본 CloudFront URL을 가리키므로 URL 불일치 → 브라우저가 preload 활용 못 함
- 해당 경고는 "LCP 점수에 포함되지 않음" → 100점에 영향 없음

### 포트폴리오 서술 방법

수치 개선 + 이유가 핵심:
> "마크다운 본문 이미지를 `<img>` 태그에서 `next/image`로 교체해 WebP 자동 변환 적용, 이미지 크기 469KB → 177KB, LCP 0.7s → 0.6s 개선"

### Vercel Root Directory 설정은 영구적

- 코드 push해도 초기화되지 않음
- 대시보드에서 수동으로 바꾸지 않는 한 유지됨

---

## 미해결

- [ ] `fetchpriority=high` 경고 완전 해소 (점수에 영향 없으므로 낮은 우선순위)
- [ ] Backend Deploy GitHub Actions 실패 (PR #30부터 이미 있던 문제, 이번 작업과 무관)
- [ ] 빈 커밋 push로 Vercel 자동 배포 설정 최종 확인
