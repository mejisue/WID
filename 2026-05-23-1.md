# 2026-05-23 — 컨텍스트 정리 #1

## 작업

### 포트폴리오 프로젝트 상세 모달 추가 (feat/project-modal)
- `ProjectModal.tsx` 신규 생성 — shadcn Dialog 기반
- `HomePage.tsx` — `PROJECT_ITEMS` / `NAV_ITEMS` 분리
- 프로젝트 이모티콘(🏠 💬 🧵) 클릭 시 외부 이탈 대신 모달 오픈
- 내비게이션(🐙 GitHub, 📒 Blog)은 기존 동작 유지
- 각 프로젝트에 기술스택 배지, 구현기능 목록, 사이트/GitHub 링크 추가
- Sleact: `https://github.com/mejisue/sleact`, Retweet: `https://github.com/mejisue/retweet`
- Thread SNS → Retweet으로 이름 변경
- develop PR #36 머지 완료

### 포트폴리오 자기소개 모달 추가 (feat/self-intro-modal)
- `SelfIntroModal.tsx` 신규 생성
- 고양이 캐릭터 클릭 → 자기소개 모달 트리거
- 내용: 이름(김지수), FullStack Developer, 자기소개 텍스트, 기술스택, 연락처(이메일/전화/GitHub), MBTI(INTJ)
- 봄 느낌 그린 컬러 팔레트 적용 (`#F4FCF6`, `#2D6A4F`, `#40916C`, `#D8F3DC`)
- `word-break: keep-all` + `overflow-wrap: break-word` 텍스트 잘림 방지
- Hydration 에러 수정 — `useState` 초기값에서 `sessionStorage` 읽기 제거, `useEffect`로 이동
- `DialogContent` 접근성 에러 수정 — `<p>` → `<DialogTitle>` 교체
- develop PR #37 머지 완료
- 다음 브랜치: `feat/svg-icon-replace` 생성 (아이패드 Procreate/Figma/Vectornator 작업 선행 필요)

### fix/favicon 브랜치 처리
- `app/layout.tsx` 미커밋 변경(favicon icons 메타데이터) 커밋 후 새 브랜치 진행

## Q&A / 배운 것

### joguman.com 기술 분석
- GSAP + Lottie 조합으로 구현된 사이트
- GSAP: 스크롤 트리거, 씬 전환, Lottie 프레임 타이밍 제어
- Lottie: 캐릭터/오브젝트 애니메이션 (After Effects → Bodymovin → JSON)
- SVG 내 `__lottie_element_` ID 패턴 = Lottie 자동 생성
- `display: block/none` 전환으로 프레임 표시
- 분석한 SVG는 문(Door) 일러스트 — 갈색 문짝 + 회색 경첩/손잡이/키패드

### Lottie란
- After Effects 애니메이션을 JSON으로 export해서 웹에서 재생하는 라이브러리
- `lottie-web` / `lottie-react` npm 패키지로 설치
- GIF보다 용량 작고 무한 확대 가능
- GSAP으로 프레임 제어 가능

### Rive란
- 웹/앱용 인터랙티브 애니메이션 제작 툴 (rive.app, 무료)
- `.riv` 바이너리 포맷 export
- Lottie 대비 장점: State Machine으로 클릭/hover 인터랙션 코드 없이 구현 가능
- Figma에서 SVG export → Rive import → 애니메이션 추가 → .riv export 워크플로우
- Next.js: `pnpm add @rive-app/react-canvas`

### SVG 아이콘 제작 툴 비교
- **Figma** (무료, 웹): 벡터 드로잉 + SVG 직접 export. 지우개 없음 — 패스 노드 삭제 또는 Boolean Subtract
- **Vectornator (Linearity Curve)** (아이패드 무료): 손그림 브러시 + 지우개 + SVG export
- **Procreate** (유료): 손그림 → PNG → vectorizer.ai → SVG 변환 필요
- 추천 워크플로우: Vectornator로 그리기 → SVG export → Figma 정리 → Next.js 적용

### Figma SVG export 방법
- Frame 선택 → 우측 패널 Export 섹션 → `+` → SVG 선택 → Export
- 배경 투명: Fill 섹션에서 `-` 버튼으로 Fill 항목 삭제 (투명도 0%와 다름)

### 문 SVG 분리 전략
- 애니메이션 있는 문짝은 별도 SVG
- 나머지 고정 요소(문틀, 바닥, 초인종)는 한 SVG
- 현재 코드의 `DoorSVG`는 GSAP `rotateY` 애니메이션 적용 중

## 미해결
- [ ] `feat/svg-icon-replace` — 아이패드 Vectornator/Figma 작업 후 이모티콘 SVG 교체
- [ ] 포트폴리오 프로젝트 모달 이미지/동영상 추가 (나중에 요청)
- [ ] `PostListClient.tsx` 기존 타입 에러 (`PostPage`, `getPostsPage`) 미수정
