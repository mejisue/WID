# 2026-06-07 — 컨텍스트 정리 #1

## 작업

### design-system Storybook 설치 및 컴포넌트 구현
- `/Users/soo/Documents/design-system` — Vite + React + TS + Tailwind v4 기반
- `pnpm dlx storybook@latest init` 으로 Storybook 10 설치
- `.storybook/preview.tsx`에 `../src/index.css` 임포트 추가 (Tailwind 토큰 연결)
- `tsconfig.app.json` + `vite.config.ts`에 `@/*` alias 추가
- `src/lib/cn.ts` — clsx 래퍼 유틸
- 구현 컴포넌트 5개:
  - **Button**: CVA 6 variants(primary·accent·outline·ghost·destructive·link) + icon size + loading + asChild(Radix Slot)
  - **Input / InputField**: size variants · error · leadingIcon/trailingIcon · useId label 자동 연결
  - **Textarea / TextareaField**: CSS field-sizing auto-resize · minRows
  - **Skeleton**: circle/block · className으로 크기 지정
  - **Avatar**: 4 sizes · src 없거나 로드 실패 시 이니셜 fallback
- 각 컴포넌트마다 `.stories.tsx` 파일 — 실제 프로젝트 패턴(retweet 로그인 폼, 프로필 수정, 포스트 로딩 등) 재현

### Storybook Vercel 배포
- `vercel.json` 추가: `buildCommand: "pnpm build-storybook"`, `outputDirectory: "storybook-static"`
- React/Next.js는 Vercel 자동 감지되지만 Storybook은 공식 지원 아니라 vercel.json 필요
- 배포 URL: **https://design-system-rouge-five.vercel.app**
- `pnpm build-storybook` → `storybook-static/` → Vercel 정적 호스팅

### design-system README 작성
- Vite 기본 템플릿 README 교체
- "설계 결정" 섹션 포함: 토큰 방식 · CVA · 컴포넌트 추출 기준(3회 반복)
- 컴포넌트 표 포함

### myWebsite Storybook 관련 코드 제거
- 현재 브랜치 `feat/sleact-modal-redesign`에 storybook이 끼어있었음
- `.storybook/`, `stories/`, `debug-storybook.log` 삭제
- `package.json` storybook 스크립트 2개 + devDependencies 8개 제거
- `.gitignore`, `eslint.config.mjs`는 git restore로 원복
- `vitest.config.ts`, `vitest.shims.d.ts` 삭제 (storybook이 생성한 파일)
- `pnpm install`로 lockfile 재생성

### myWebsite develop → main 머지
- develop이 main보다 4커밋 앞, main도 develop에 없는 2커밋 있음 (diverged)
- `git pull origin develop` 실패 원인: fast-forward 불가 구조
- 올바른 방법: `git checkout main` → `git merge origin/develop` → `git push origin main`
- merge 중 editor 오류(`code --wait: command not found`) → `git commit --no-edit`으로 해결
- 머지 상태가 pending이어서 push 후 파일 미반영 → `git commit -m "Merge branch 'develop'"` 후 재push

### myWebsite design-system 프로젝트 카드 추가 (feat/add-storybook-section)
- `ProjectModal.tsx` links 타입에 `storybook?: string` 추가
- Storybook 버튼 렌더링 (색상: `#FF4785` — Storybook 브랜드 컬러)
- `HomePage.tsx` project4 추가: 🖼️ Design System, `top: 15%, right: 10%` 위치
  - 설계 결정 3개, features 5개, storybook + github 링크
- PR #42 → develop 머지 완료
- 이후 흐름: `git checkout develop` → `git pull origin develop` → `git checkout main` → `git merge develop` → `git push origin main`

## Q&A / 배운 것

### Storybook 아키텍처 — 어디에 설치하나
- Storybook은 design-system 하나에만 설치
- myWebsite / retweet / sleact는 나중에 design-system 패키지를 import하는 consumer
- 현재 목적: 포트폴리오용 (Storybook 배포 링크)

### CVA (class-variance-authority)
- variant × size 조합을 if문 대신 선언형 테이블로 관리
- `VariantProps<typeof button>` → TypeScript 타입 자동 추론
- 없는 variant 넣으면 컴파일 에러로 차단

### 현업 컴포넌트 추출 기준
- 3회 이상 반복된 패턴
- Figma 컴포넌트와 1:1 대응
- 접근성 처리가 복잡한 것 (Dialog, Select 등)
- 변경 범위가 넓은 것 (브랜드 색 들어간 것)
- 제외: 페이지 레이아웃, 비즈니스 로직 포함, 1회 사용

### 현업 디자인시스템 적용 방식
- **npm 패키지 배포**: `@toss/tds`, `bezier-react` 등 — 버전 관리 가능
- **모노레포**: packages/ui + apps/* — Turborepo/Nx 활용
- **복사 붙여넣기(shadcn)**: 패키지 install 없이 코드 복사, 완전한 소유권

### Storybook Vercel 배포 시 vercel.json 필요한 이유
- React/Next.js는 Vercel 공식 지원 프레임워크 → 자동 감지
- Storybook은 공식 지원 X → buildCommand, outputDirectory 직접 지정 필요

### git diverged 브랜치 머지
- `git pull origin develop` (main에서) 실패 → fast-forward 불가
- `git merge origin/develop` 사용 (머지 커밋 생성)
- editor 오류 시 → `git commit --no-edit`
- 올바른 순서: checkout main → pull origin main → merge develop → push origin main

## 미해결
- [ ] design-system GitHub 저장소 연결 (현재 로컬 git만 있음)
- [ ] (갭) Sentry/Amplitude 모니터링 적용
- [ ] (갭) retweet 모달 5역량 구조로 코드 적용
