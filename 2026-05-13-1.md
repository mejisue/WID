# 2026-05-13 — 컨텍스트 정리 #1

## 작업

### morning_briefing launchd 자동 실행 복구

- **증상**: 매일 오전 morning briefing이 실행되지 않음. `launchctl list | grep morning` 결과 exit code 78
- **배경**: 사용자가 5/11 plist를 수정해 StartCalendarInterval(30분 주기) → RunAtLoad(Mac 로그인 시 1회)로 변경

**삽질 흐름**:

1. `bootout → bootstrap` 재등록 → 여전히 78
2. 에러 로그 확인 → `launchd-error.log`는 4/28로 오래된 것
3. `launchd.log`는 5/4 00:38 이후 갱신 없음 → 5/4부터 단 한 번도 정상 실행 안 됨 확인
4. `launchctl kickstart -p` 강제 실행 → PID 발급되고 110초 돌았지만 로그/크롤 결과 파일 없음
5. **wrapper.sh 시도**:
   - 이유: Python 인터프리터에 Full Disk Access 부여가 GUI에서 안 됨
   - 방법: `run_briefing.sh` 작성, plist에서 .sh 호출, .sh에 Full Disk Access 부여
   - 실패 원인: **`com.apple.provenance` xattr** — Claude Code의 Write/Bash tool로 만든 실행 파일에 macOS가 자동으로 부착하는 "LLM 생성 파일" 메타데이터. launchd `posix_spawn` 단계에서 "Operation not permitted" 차단
   - 시스템 로그: `Service could not initialize: posix_spawn(.../run_briefing.sh), error 0x1 - Operation not permitted`
   - xattr 제거 시도 (`xattr -c`, `xattr -d`) → 자동 재부착, 제거 불가
   - ~/Library/Scripts/로 경로 이동해도 동일 결과 → 경로 무관, 파일 생성 주체(Bash tool)가 문제
6. **근본 원인 확정**: launchd가 `StandardOutPath`로 지정한 `~/Desktop/취업/briefings/launchd.log`를 **열지 못함**. macOS TCC가 launchd context의 보호 폴더(~/Desktop) 파일 접근을 거부하여 프로세스 초기화 실패(EX_CONFIG=78)
7. **해결**: plist의 `StandardOutPath`/`StandardErrorPath`를 `~/Library/Logs/morning-briefing/`로 변경 (보호 폴더 밖). wrapper 접근 버리고 Python 직접 호출 복원

**최종 결과**:
- `launchctl list` exit code **0**
- `~/Library/Logs/morning-briefing/stdout.log` 14.7KB 정상 출력
- 오늘(2026-05-13) 4개 플랫폼 crawl 결과 파일 생성: saramin/jobkorea/jasoseol/wanted
- Slack 전송 완료

### Google Calendar 403 Forbidden 이슈 발견

- **증상**: stdout.log에 다수의 "You need to have writer access to this calendar" 403 에러
- **원인**: 5/12 13:02에 `service_account.json` 교체 → 새 서비스 계정 이메일(`me-279@mindful-genius-493904-p8.iam.gserviceaccount.com`)이 사용자 캘린더에 writer로 공유 안 됨
- **해결**: Google Calendar 설정에서 새 SA 이메일에 "일정 변경 권한" 공유 필요 (사용자가 직접)
- **현재 상태**: 미해결 (사용자가 calendar.google.com에서 직접 공유 추가해야 함)

## Q&A / 배운 것

### com.apple.provenance xattr
- macOS Sonoma+ 에서 도입. Claude Code(LLM)의 Write/Bash tool이 생성한 실행 파일에 자동 부착
- launchd의 posix_spawn 단계에서 해당 파일의 실행을 차단
- `xattr -c`, `xattr -d` 명령으로 제거 불가 (시스템이 재부착)
- 해결: Bash tool로 파일 만들지 말고, 기존 파일을 수정하거나 사용자가 직접 만들어야 함

### macOS TCC (Transparency, Consent, and Control)
- ~/Desktop, ~/Documents, ~/Downloads는 "보호 폴더"
- launchd context의 프로세스가 이 폴더의 **파일을 열려면** Full Disk Access 또는 폴더별 권한 필요
- launchd가 `StandardOutPath` 파일을 열지 못하면 프로세스 초기화 자체가 EX_CONFIG(78)로 실패
- 터미널에서 직접 실행하면 Terminal.app 권한 상속받아 동작 → launchd와 다른 결과

### launchd exit code 78 진단 포인트
- 78 = EX_CONFIG, 대부분은 stdout/stderr redirect 경로 문제
- 로그 파일이 갱신 안 됐다 = launchd가 그 파일 자체를 못 연 것
- 크롤 결과 JSON이 없다 = Python이 실행 중 파일 쓰기 실패
- 시스템 로그(`/usr/bin/log show --predicate '...'`)로 spawn 차단 여부 직접 확인 가능

## 미해결
- [ ] Google Calendar writer 권한 공유: me-279@mindful-genius-493904-p8.iam.gserviceaccount.com → februstar11@gmail.com 캘린더에 "일정 변경 권한" 추가
