# 2026-05-12 — 컨텍스트 정리 #1

## 작업

### 구글 캘린더 [마감] 공고 일괄 탈락 처리
- 5월 11일, 5월 12일 캘린더 이벤트 중 `[마감]` 태그가 붙은 공고 7건 탈락 처리 요청
- Google Calendar MCP `list_events`로 해당 기간 이벤트 조회
- Python으로 지원-트래커.md에서 공고 URL 기반으로 7건 상태 `작성중 → 탈락` 변경
- Google Calendar MCP `delete_event`로 7개 이벤트 삭제
- 처리 목록: 서린바이오사이언스, 에스유스카우트, 카카오뱅크, 알서포트, 대한지방행정공제회, 세기정보통신, 한국전자기술연구원

### 모닝브리핑 launchd 설정 수정
- `launchd-error.log`에서 `invalid_grant: Token has been expired or revoked` 에러 확인
- 근본 원인: Google Cloud Console이 Test 모드일 때 발급된 OAuth 토큰은 7일마다 만료됨. Production 전환 후에도 기존 토큰은 그대로 만료됨 → 토큰 삭제 + 재인증 필요
- 기존 plist: `StartCalendarInterval` 9/10/11/12시 4개 + `RunAtLoad true` (하루 최대 5회 시도)
- 변경: `StartCalendarInterval` 전부 제거, `RunAtLoad true`만 유지 → 맥 켤 때 1회만 실행
- `launchctl unload → load`로 재적용
- 재인증 방법: `python3 morning_briefing.py --force` 터미널 직접 실행 → 브라우저 OAuth → token.json 재생성
- 테스트: `launchctl start com.soo.morning-briefing`으로 즉시 실행 가능

### 프론트엔드 공고 역량 갭 분석
- `/Users/soo/Documents/경험-블록-통합.md` 기반 현재 역량 파악
- 지원-트래커.md에서 프론트엔드 공고 102건(탈락/포기 제외) 필터링
- 보충 필요 항목 우선순위:
  1. Jest + RTL 테스트 코드 (경험 블록 전체에 없음 — 치명적)
  2. 번들 최적화 수치 확보 (LCP 수치만 있고 번들 분석 없음)
  3. Redux Toolkit 개념 정리 (Zustand만 있음)
  4. ErrorBoundary / Suspense 패턴
  5. React Native 기초 경험
  6. a11y 서술 보강

### Retweet 번들 최적화 (perf/bundle-optimization → develop → origin push)
- **Before**: JS 단일 청크 510.46 kB (gzip 164 kB), Vite 경고 발생
- 브랜치 `perf/bundle-optimization` 생성
- 변경 1 — `root-route.tsx`: 모든 페이지 정적 import → `React.lazy()` + `Suspense` 전환
- 변경 2 — `vite.config.ts`:
  - `rollup-plugin-visualizer` 추가 (빌드 시 stats.html 자동 생성)
  - `manualChunks` 함수로 벤더 청크 분리: `vendor-react` / `vendor-query` / `vendor-ui`
  - Vite v8은 Rolldown 사용 → `manualChunks`가 객체 아닌 함수여야 함 (에러 후 수정)
- **After**: 15개 청크 분리, 초기 로드 gzip 164 kB → 34 kB (79% 감소)
- `develop` fast-forward 머지 → `origin/develop` push 완료

## Q&A / 배운 것

### Google OAuth invalid_grant 근본 원인
- Test 모드 앱의 refresh token은 7일 만료 정책 적용
- Production 전환해도 이미 발급된 토큰에는 소급 적용 안 됨
- 해결: 토큰 삭제 + Production 상태에서 재인증

### 번들 최적화 개념
- **코드 스플리팅(lazy)**: 초기 로딩 시 필요한 JS만 다운로드, 나머지는 해당 페이지 접근 시
- **벤더 청크 분리**: 라이브러리(node_modules)를 별도 청크로 분리
  - 내 코드만 변경해서 재배포해도 라이브러리 청크 파일명 유지 → 브라우저 캐시 재사용
  - 재방문 사용자는 변경된 내 코드 청크만 새로 다운로드
- **Vite v8 주의**: Rolldown 기반으로 전환됨, `manualChunks`는 객체가 아닌 함수로 작성해야 함

### launchd RunAtLoad 동작
- `RunAtLoad true`: agent 로드 시(맥 부팅/로그인) 1회 실행
- `StartCalendarInterval` 없이 `RunAtLoad`만 있으면 맥 켤 때 1회만 실행
- morning_briefing.py는 `crawl_results/saramin-{today}.json` 파일 존재 여부로 오늘 이미 실행됐는지 체크하는 로직 내장

## 미해결
- [ ] morning_briefing.py 재인증 (token.json 삭제 후 터미널에서 --force 실행 필요)
- [ ] Retweet RTL 테스트 코드 작성 (경험 블록 보강용)
