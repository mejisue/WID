# 📅 2026-06-10 — WID

## 🛠️ 작업

### 🔗 retweet — Sentry 에러 모니터링 도입 (FE + BE)
- 프론트: `feat/sentry-frontend` 브랜치 push → PR #25
  - `@sentry/react` + `@sentry/vite-plugin`, `src/sentry.ts`(React Router v7 통합), `main.tsx` React 19 에러 핸들러, axios 인터셉터 에러 캡처, 소스맵 업로드 설정
- 백엔드: `feat/sentry-backend` 브랜치 → PR #26
  - `sentry-spring-boot-starter-jakarta` 추가, `application.yaml`에 DSN/environment/traces-sample-rate 설정
  - `GlobalExceptionHandler`의 catch-all `Exception` 핸들러에 `Sentry.captureException(e)` 직접 호출 (자동 캡처가 안 되는 구조라서)
- 운영 환경 반영
  - Vercel: `VITE_SENTRY_DSN` 환경변수 등록
  - EC2: `.env.prod`에 `SENTRY_DSN` 추가 후 `docker compose --env-file .env.prod -f compose.prod.yaml down/up -d`로 재배포
- 검증: 백엔드 깨진 JSON 요청 → catch-all → Sentry 캡처 확인 / 프론트 콘솔 에러 → Sentry 반영 확인
- ✅ PR #25, #26 모두 main 머지 완료, 운영 Sentry 연동 완료

### 📊 retweet — Amplitude 사용자 행동 분석 도입
- `feat/amplitude-frontend` 브랜치 → `@amplitude/analytics-browser` 도입
- `src/amplitude.ts`(init, autocapture는 pageViews/sessions만), `src/lib/analytics.ts`(AMPLITUDE_EVENTS 상수 + trackEvent/identifyUser/resetUser)
- 핵심 이벤트 6종 트래킹: `sign_up`, `sign_in`(password/github), `sign_out`, `post_created`, `post_like_toggled`, `comment_created`
- "익명 사용자로만 잡히는" 문제 → `identifyUser(memberId)` / `resetUser()`로 로그인 연동 해결
- PR #27 생성 (develop 대상), Test plan에는 typecheck/lint 빼고 실동작 검증만 작성

### 🐛 Vercel 배포 에러 — "vite: command not found" (exit 127)
- 처음엔 Amplitude PR 영향 의심했으나 빌드 로그 비교로 무관 확인
- 원인: Vercel 프로젝트 설정의 **Root Directory가 `front` → `.`(루트)로 변경**되어 있었음 → install 단계가 빈 채로 끝나고 vite 미설치
- 해결: Root Directory를 `front`로 되돌리고 재배포 필요 (안내 완료)

### 🗂️ 포트폴리오 경험 블록 보강 (28개) + JD 매칭 분석
- `경험-블록-인덱스.md` / `경험-블록-통합.md`에 신규 블록 6개 추가 (A~W 23개 → 28개)
  - **X**: Sentry FE+BE 통합 모니터링(401 제외 캡처) + Amplitude 6종 이벤트
  - **Y**: 비밀번호 재설정 — Retweet(DB 토큰 1시간) vs Sleact(Redis TTL 30분), 둘 다 AWS SES
  - **Z**: Sleact 워크스페이스/채널 멤버 초대 — 조인 엔티티 + 기본 채널 자동 가입
  - **AA**: myWebsite 마크다운 에디터+렌더링 파이프라인, 첫 이미지 preload로 LCP 최적화
  - **AB**: 3개 프로젝트 공통 백엔드 아키텍처 패턴(Auditing, N+1 방지, 페이지네이션, 전역 예외, Stateless 보안)
  - **AC**: 3개 프로젝트 백엔드 테스트 30개 클래스 (계층별 4가지 전략)
- 메모리 갱신: `project_wishlist_gap.md`, `MEMORY.md`에 완료 현황 반영
- `/Users/soo/Desktop/취업/front_jd_매칭분석.md` 신규 작성 — 28개 회사 JD vs 28개 경험 블록 대조, 공통 어필포인트 Top 10 / 보완점 / 회사별 매칭도 정리

### 🎨 myWebsite — 폰트 Nanum Gothic 적용 + html lang="ko"
- `app/layout.tsx`에 `next/font/google`의 `Nanum_Gothic`(700/800) 추가, `--font-nanum-gothic` 변수 등록
- `globals.css`의 `@theme inline`에서 `--font-sans` 교체
- 문제: `--font-sans`만 바꿔서는 적용 안 됨
  - 원인: next/font 변수는 `body`에 정의되는데 Tailwind preflight의 `--default-font-family`는 `:root`(html) 스코프 → 변수 상속 안 됨
  - 해결: `@layer base`의 `body`에 `font-sans` 유틸리티 직접 적용
- 추가로 Turbopack HMR 캐시 문제 → `.next` 삭제 후 재시작으로 정상화
- `<html lang="en">` → `<html lang="ko">` 변경
- ✅ 커밋 `2187e6c`, main에 push 반영 확인 완료

### 📄 이력서(CV) 풀스택 버전 제작 + myWebsite 반영
- 기존 `cv.pdf`(5p) 구조 분석 → 백엔드/풀스택/프론트엔드 모두 지원 목적에 맞게 재설계
- 의사결정: **직무 프리셋 토글**(BE/풀스택/FE 버튼으로 핵심역량·프로젝트·불릿 순서 자동 재정렬) + AI 채용공고 자동화 프로젝트 추가
- `경험-블록-인덱스.md`/`경험-블록-통합.md`(블록 A~AC, S, V, W = 27개)를 출처로 모든 프로젝트 불릿 작성
- `/Users/soo/Desktop/cv/` 생성: `data.js`(콘텐츠) / `style.css` / `render.js`(직무별 재정렬) / `index.html` / `README.md`
- Node로 데이터 검증 → BE 11 / FE 11 균형 확인
- 포트폴리오 사이트엔 **풀스택 단일 버전**만 올리기로 결정 (토글 도구는 직무별 PDF 추출용으로 유지)
- 사용자 요청 수정 반영: Python 제거, Sleact 소개 "클론/Slack" 표현 삭제, AI 자동화 스택을 Claude Code만 남김, "일 400건+"→"일 평균 10건 이상", 사진(photo.JPG) 반영
- headless Chrome으로 `cv-fullstack.pdf` 생성
- `myWebsite`에서 `chore/update-cv-pdf` 브랜치 생성 → `front/public/cv.pdf` 교체 → 커밋(`6bd9c61`) → main에 fast-forward 머지
- ⚠️ 로컬 main에만 반영, **origin/main push는 아직 안 함**

## 💡 배운 것 / Q&A

### Spring Boot Sentry — catch-all 핸들러와 자동 캡처
- `sentry-spring-boot-starter`는 설정만으로 자동 캡처되지만, `@ExceptionHandler(Exception.class)` catch-all이 있으면 "이미 처리된 예외"로 인식되어 자동 캡처가 안 됨 → 핸들러 안에서 `Sentry.captureException(e)` 수동 호출 필요

### Amplitude란?
- 사용자 행동(이벤트) 기반 제품 분석 도구. Sentry는 "에러", Amplitude는 "무엇을 했는지"를 추적
- Event / Event Properties / User Properties로 퍼널·리텐션 분석

### Vite 환경변수 & Vercel
- `import.meta.env.VITE_*` 형태만 클라이언트 번들에 포함되며 빌드 타임에 박힘 → 환경변수 추가 후 반드시 재배포 필요

### docker compose env_file
- `env_file:`로 지정한 키=값은 compose 파일에 별도 명시 없이 컨테이너 환경변수로 그대로 주입됨. 변경 후 적용하려면 컨테이너 재생성 필요

### next/font + Tailwind v4 폰트 적용 메커니즘
- next/font의 `.variable` 클래스는 `body`에 위치 → `:root`(html) 스코프의 `--default-font-family`(`var(--font-sans)`)에서는 해당 변수를 못 찾음 → `body`에 `font-sans` 유틸 직접 적용해야 함
- `lang` 속성은 폰트와 무관한 메타데이터(스크린리더/SEO/번역 기능에 영향)

### 경험 블록 통합 문서의 가치
- `경험-블록-인덱스.md`/`경험-블록-통합.md`는 27개(이후 28개) STAR 경험 블록 정본 — 이력서/자소서 작성 시 코드 재탐색 없이 1차 소스로 활용 가능

## ✍️ 오늘 추가로 한 것
- 코드잇 프리코스 — 프리렌더링까지 수강 완료

## 📌 미해결
- [ ] Vercel 프로젝트 Root Directory를 `front`로 재설정 후 재배포 (현재 main/PR 배포 실패 상태)
- [ ] PR #27(Amplitude) 머지 후 Vercel에 `VITE_AMPLITUDE_API_KEY` 환경변수 등록
- [ ] (선택) 2차 Amplitude 이벤트(post_updated/deleted, profile_updated 등) 추가 여부 결정
- [ ] front_jd_매칭분석.md 기반으로 자소서/이력서에 어필 포인트 추가 반영
- [ ] (선택) JD 보완점(E2E 테스트, GraphQL 등) 우선순위 정해서 학습/구현 여부 결정
- [ ] myWebsite main 브랜치 origin/main push (cv.pdf 교체분 미반영)
- [ ] Sentry 테스트로 발생시킨 더미 이슈들 대시보드에서 정리
- [ ] cv 사진(photo.JPG) 비율/잘림 등 디자인 미세 조정 여부 확인
