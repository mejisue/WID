# 2026-06-06 — 컨텍스트 정리 #1

## 작업

### myWebsite 미니 디자인 시스템 구현
- `globals.css`에 `--success` / `--warning` / `--info` 시맨틱 토큰 추가 (light/dark 각각)
- `lib/tokens.ts` 신규 생성: CSS 커스텀 프로퍼티를 TypeScript 상수로 문서화
- `components/ui/badge.tsx`에 `success` / `warning` / `info` variant 3개 추가
- Storybook 설치 (`pnpm dlx storybook@latest init`) → `@storybook/nextjs-vite` 프레임워크로 설정됨
- `.storybook/preview.tsx`에 `import '../app/globals.css'` 추가 (없으면 Tailwind 토큰 미적용)
- `globals.css` import 순서 수정: `@import "shadcn/tailwind.css"`를 `@plugin "tailwindcss-animate"` 앞으로 이동 (PostCSS @import 순서 규칙)
- 보일러플레이트 스토리 삭제 후 `stories/Button.stories.tsx`, `stories/Badge.stories.tsx` 작성
- Storybook 실행: `localhost:6006`

### 한계 확인
- 앱에서 Badge를 실제로 사용하는 곳이 없음 (`grep` 결과 0건)
- `components/ui`에서 앱이 import하는 건 `dialog` 하나뿐
- 추가한 success/warning/info 토큰이 렌더링되는 페이지 없음
- 포트폴리오 서술 시 "단순 추가"가 아니라 실제 사용처를 만들어야 설득력 있음

## Q&A / 배운 것

### FE에서 디자인 시스템 설계·운영이 필요한 이유
- 단일 진실 공급원(single source of truth): 같은 버튼이 코드베이스 여러 곳에 제각각 존재하는 문제 해결
- 토큰: 색/간격 등 디자인 결정을 이름 있는 변수로 추상화 → 의미 분리, 다크모드 전환, 디자이너-개발자 공통 언어
- variant: 컴포넌트 사용처가 스타일을 결정하지 않고 의도(primary, danger 등)만 전달 → 변경이 컴포넌트 내부로 격리됨
- 실무 비용: 토큰 없으면 색 변경 시 전체 grep 필요, variant 없으면 조건부 className 지옥

### Storybook 개념
- 앱과 완전히 분리된 환경에서 컴포넌트를 독립적으로 개발·확인·문서화하는 도구
- Story = 컴포넌트의 특정 상태 하나
- CSF(Component Story Format): `default export = meta`, `named export = 각 story` 구조가 표준
- `args`: 단일 컴포넌트에 props 전달, Controls 패널에서 실시간 변경 가능
- `render`: 여러 컴포넌트를 자유 배치할 때 사용 (AllVariants 같은 케이스)
- `argTypes`: Controls 패널 UI 커스터마이징 (select/boolean 등)
- `tags: ['autodocs']`: props 타입 자동 읽어서 Docs 페이지 생성
- `preview.tsx`: 모든 스토리에 적용되는 전역 설정, Next 프로젝트는 여기서 CSS import 필수
- `name` 필드: 변수명과 다른 이름을 사이드바에 표시할 때 (한국어 가능)

### React SPA vs Next.js 선택 기준
- Next가 강요하는 것: Node 서버 필요, 파일 기반 라우팅, 서버/클라이언트 경계, 빌드 복잡도
- React SPA를 선택하는 이유
  1. 정적 파일만 배포하는 환경 (S3+CloudFront, BE 팀이 서버 운영)
  2. SEO 불필요한 서비스 (어드민, 사내 도구, 로그인 후에만 접근)
  3. 실시간 데이터 (주식 차트, 라이브 대시보드 — SSR 초기 HTML이 의미 없음)
  4. 팀에 Next 운영 경험 없을 때 리스크
  5. Vite + React의 빌드 속도 (대형 프로젝트에서 체감)
- 한 줄: 서버 있고 + SEO 중요하고 + 초기 로드 중요하면 Next, 그 조건 없으면 React SPA

## 미해결
- [ ] Badge / 새 토큰이 실제 앱 페이지에서 사용되도록 적용 (포스트 카테고리 태그, 기술 스택 뱃지 등 자연스러운 위치 찾기)
- [ ] 디자인 시스템 설계 경험을 포트폴리오에 어떻게 서술할지 확정
- [ ] 앱 내 LLM API 직접 연동 기능 1개 추가 (AI Native 증명)
- [ ] 핵심역량 7줄 → 직무별 5줄 버전 확정
- [ ] 렌더링 전략(ISR/SSG) 의도적 구분 적용 경험 정리
