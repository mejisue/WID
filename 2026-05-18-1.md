# 2026-05-18 — 컨텍스트 정리 #1

## 작업

### 상태 변경 (탈락 처리)
- 모니터랩(사람인), 파크시스템스(주) 2건, 에이케이플라자(주), 굿블록, 베어로보틱스코리아, 모드픽, 에이엑스, 선진 → 탈락
- 5/18~5/19 마감 공고 10건 일괄 탈락 처리
- 각각 Google Calendar 이벤트 삭제 + calendar_event_map.json 항목 제거

### 경험풀 Vitest 블록 추가 (블록 T)
- retweet/front, sleact/front 테스트 파일 전수 분석
- vi.mock, vi.hoisted, vi.stubGlobal, userEvent, MemoryRouter, Zustand 직접 테스트 패턴 확인
- 경험-블록-통합.md에 블록 T 추가, 경험-블록-인덱스.md 반영

### 위시스트 JD 갭 분석
- JD 원문: SNS 미디어 큐레이팅, 대량 미디어 렌더링, 산점도 시각화, Sentry/Amplitude, 디자인 시스템
- 스택: TypeScript, React, Zustand, TanStack Query, Mantine, Rspack/ESBuild
- 갭: 번들 분석 경험, 가상화, Sentry/Amplitude, 디자이너 협업

### 번들 최적화 + 블록 M 업데이트
- retweet/front pnpm build 실행 → rollup-plugin-visualizer 수치 확보
  - vendor-react: 76kB gzip (전체의 41.9%), 총 raw 554kB / gzip 182kB
  - manualChunks: vendor-react / vendor-query / vendor-ui 3도메인 분리
  - 페이지별 청크: 0.2~5kB 수준으로 분산
- 블록 M을 "번들 최적화 + Server Components 기반 성능 최적화 및 SEO"로 확장

### 가상화 구현 (블록 U)
- 브랜치: perf/post-feed-virtualization (retweet/front, develop 기준)
- @tanstack/react-virtual 설치 (pnpm add)
- PostFeed에 useWindowVirtualizer 적용
  - useVirtualizer(컨테이너 스크롤) 아님, useWindowVirtualizer(window 스크롤) 사용
  - scrollMargin: listRef.current?.offsetTop — 헤더 오프셋 보정
  - measureElement: 가변 높이 아이템 실측
  - overscan: 3 — flash 방지
  - getTotalSize(): 스크롤바 높이 확보
  - transform: translateY(start - scrollMargin) — 피드 컨테이너 기준 배치
- VirtualFeed 컴포넌트 분리 (postIds 확정 후 초기화 보장)
- vite.config.ts manualChunks에 @tanstack/react-virtual → vendor-query 추가
  - index-page 청크: 18.44kB → 2.45kB로 감소
- 경험-블록-통합.md 블록 U 추가, 인덱스 반영

---

## Q&A / 배운 것

### Mantine이 뭐야?
- React용 UI 컴포넌트 라이브러리 (shadcn/ui, MUI와 같은 레이어)
- 컴포넌트 + 훅 동시 제공 (useForm, useDisclosure 등 내장)
- CSS Modules 기반, v7부터 CSS 변수로 테마
- headless 아님 — 스타일이 입혀진 채로 제공

### 번들 사이즈 / 렌더링 지표 기반 성능 최적화가 필요한 이유
- 개발자 환경(맥북+기가인터넷)과 실제 사용자 환경(LTE+구형폰) 차이
- 측정 없이는 느린지조차 모름 → rollup-plugin-visualizer, Lighthouse, Core Web Vitals
- Core Web Vitals: LCP(2.5초 이내), CLS(0.1 이하), INP(200ms 이내)
- vendor-react 41.9% 의미: React 관련 코드가 전체 번들의 41.9%, 변경 빈도 낮으므로 분리하면 캐시 효율 극대화
- 캐시 hit 확인: Chrome DevTools Network 탭 → Status가 (disk cache)이면 캐시 hit

### 브라우저가 화면 밖 DOM을 전부 만드는 이유
- DOM 트리 생성은 화면 가시성 무관하게 전체 요소를 메모리에 올림
- 500개 렌더 시: 500개 DOM 노드 생성 + 이미지 500장 로드 + 스타일/레이아웃 계산
- 증상: 초기 로딩 느림, 스크롤 버벅임, 메모리 폭발

### 가상화(Virtualization) 개념
- "어차피 보이는 건 3~4개뿐이니 DOM에도 3~4개만 만들자"
- 바깥 컨테이너를 getTotalSize() 높이로 확보 → 스크롤바 정상
- 내부 아이템은 position:absolute + translateY로 각자 위치에 배치
- 스크롤 시 멀리 간 아이템 제거 + 새로 보이는 아이템 추가 (DOM 재사용)

### 가상화 vs 무한 스크롤 차이
- 무한 스크롤: 서버↔JS 메모리 구간 담당 (데이터를 언제 더 가져올까)
- 가상화: JS 메모리↔DOM 구간 담당 (가져온 데이터를 DOM에 어떻게 보여줄까)
- 두 개는 독립적 — 같이 써도 되고 따로 써도 됨

### useVirtualizer vs useWindowVirtualizer
- useVirtualizer: 특정 div(overflow:auto)가 스크롤되는 구조
- useWindowVirtualizer: window 전체가 스크롤되는 구조

### scrollMargin(오프셋) 개념
- 오프셋: 기준점에서 얼마나 떨어져 있는지
- virtualizer의 start는 페이지 전체 기준 y좌표 → 피드 컨테이너 기준으로 변환 시 오프셋 차감 필요
- scrollMargin 없으면 아이템이 헤더 뒤에 겹쳐 배치됨

### 무한 스크롤 + 가상화 충돌 여부
- 충돌 아님 — 스크롤바 길이가 새 데이터 로드 후 늘어나는 건 의도된 동작
- 실질적 문제: estimateSize < 실제 높이이면 bottomRef가 너무 일찍 감지됨 → fetchNextPage 조기 호출
- 해결: estimateSize를 실제 평균 높이에 가깝게 조정

### virtualizer의 역할
- 직접 DOM을 건드리지 않고 숫자만 계산해서 제공하는 계산기
- getTotalSize() → 전체 컨테이너 높이
- getVirtualItems() → 지금 DOM에 있어야 할 아이템 목록(index, start, key)

### 한양대 전산개발 JD 분석
- 데이터베이스 프로그램 개발 가능자 = Oracle SQL / PL-SQL 작성 가능자
- 레거시 스택: Oracle, Java EE/Spring Framework, jQuery, HTML5+AJAX
- 경험풀 대응: Spring Boot(블록 D·F), Linux/Docker(블록 O) 커버, Oracle·jQuery 갭
- 지원 전략: Linux 배포·트러블슈팅 + Spring Boot 설계 경험 전면에, 학습 의지 어필

---

## 미해결
- [ ] Sentry 연동 — 에러 트래킹, sourcemap 연동 (내일 아침 예정)
- [ ] Amplitude 연동 — 사용자 행동 분석, 이벤트 트래킹 (내일 아침 예정)
- [ ] perf/post-feed-virtualization 브랜치 커밋·PR 미완료
- [ ] estimateSize 실제 평균 높이 측정 후 값 조정 (Chrome DevTools 콘솔로 확인)
